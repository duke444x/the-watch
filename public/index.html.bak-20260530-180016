<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Capt's Ledger · The Watch</title>
  <link rel="icon" href="/captcrawl.png" />
  <style>
    /* ─── Brand palette ────────────────────────────────────────────────── */
    :root {
      --bg-deep:        #08090d;
      --bg-card:        #14171f;
      --bg-card-hi:     #1a1f2b;
      --border:         #2a2f3b;
      --border-hi:      #3a4150;
      --text-primary:   #e6edf3;
      --text-muted:     #8b949e;
      --text-dim:       #6e7681;
      --teal:           #2DD4BF;
      --teal-glow:      rgba(45, 212, 191, 0.15);
      --coral:          #FB7185;
      --coral-glow:     rgba(251, 113, 133, 0.15);
      --gold:           #F9C015;
      --gold-glow:      rgba(249, 192, 21, 0.15);
      --rebel-red:      #E60000;
      --sky:            #61A6DD;
      --mono: 'SF Mono', 'JetBrains Mono', Menlo, Monaco, 'Cascadia Mono', 'Courier New', monospace;
      --display: ui-serif, Georgia, 'Times New Roman', serif;
    }

    /* ─── Reset / base ─────────────────────────────────────────────────── */
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body {
      background: var(--bg-deep);
      color: var(--text-primary);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      font-size: 14px;
      line-height: 1.5;
      min-height: 100vh;
      background-image:
        radial-gradient(ellipse 80% 40% at 50% -10%, rgba(249, 192, 21, 0.05) 0%, transparent 70%),
        radial-gradient(ellipse 60% 30% at 50% 110%, rgba(45, 212, 191, 0.04) 0%, transparent 70%);
      background-attachment: fixed;
    }
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: repeating-linear-gradient(
        135deg,
        transparent 0,
        transparent 24px,
        rgba(255, 255, 255, 0.008) 24px,
        rgba(255, 255, 255, 0.008) 25px
      );
      pointer-events: none;
      z-index: 0;
    }
    .wrap { position: relative; z-index: 1; }
    a { color: var(--teal); text-decoration: none; }
    a:hover { text-decoration: underline; }

    .mono { font-family: var(--mono); }
    .pos  { color: var(--teal); }
    .neg  { color: var(--coral); }
    .dim  { color: var(--text-dim); }
    .muted { color: var(--text-muted); }

    /* ─── Layout ──────────────────────────────────────────────────────── */
    .wrap {
      width: 100%;
      max-width: 1600px;
      margin: 0 auto;
      padding: 24px 32px 60px;
    }
    @media (max-width: 640px) {
      .wrap { padding: 18px 18px 48px; }
    }

    /* ─── Hero banner ─────────────────────────────────────────────────── */
    .hero {
      position: relative;
      height: 340px;
      border-radius: 14px;
      overflow: hidden;
      margin-bottom: 28px;
      border: 1px solid var(--border);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
    }
    .hero-image {
      position: absolute;
      inset: 0;
      background-image: url('/plank-hero.png');
      background-size: cover;
      background-position: center 60%;
      transition: transform 8s ease-out;
    }
    .hero:hover .hero-image {
      transform: scale(1.04);
    }
    .hero-overlay {
      position: absolute;
      inset: 0;
      background:
        linear-gradient(to bottom,
          rgba(8, 9, 13, 0.3) 0%,
          rgba(8, 9, 13, 0) 30%,
          rgba(8, 9, 13, 0.4) 65%,
          rgba(8, 9, 13, 0.95) 100%);
    }
    .hero-content {
      position: absolute;
      bottom: 28px;
      left: 36px;
      right: 36px;
      z-index: 2;
    }
    .hero h1 {
      font-family: var(--display);
      font-size: 52px;
      font-weight: 600;
      color: var(--gold);
      letter-spacing: -1px;
      line-height: 1;
      text-shadow:
        0 0 30px rgba(249, 192, 21, 0.4),
        0 2px 20px rgba(0, 0, 0, 0.8);
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 14px;
    }
    .hero h1 .pirate {
      font-size: 56px;
      filter: drop-shadow(0 0 16px rgba(249, 192, 21, 0.55));
      line-height: 1;
      animation: flag-wave 4s ease-in-out infinite;
    }
    @keyframes flag-wave {
      0%, 100% { transform: rotate(-2deg); filter: drop-shadow(0 0 16px rgba(249, 192, 21, 0.55)); }
      50%      { transform: rotate(2deg);  filter: drop-shadow(0 0 24px rgba(249, 192, 21, 0.75)); }
    }
    .hero .tagline {
      color: var(--gold);
      font-family: var(--display);
      font-size: 17px;
      font-weight: 500;
      letter-spacing: 0.2px;
      text-shadow: 0 1px 12px rgba(0, 0, 0, 0.95);
      margin-bottom: 10px;
      font-style: italic;
      opacity: 0.95;
    }
    .hero .credits {
      color: rgba(230, 237, 243, 0.7);
      font-size: 11.5px;
      font-family: var(--mono);
      letter-spacing: 0.4px;
      text-shadow: 0 1px 8px rgba(0, 0, 0, 0.9);
      display: flex;
      flex-wrap: wrap;
      align-items: baseline;
      gap: 2px 8px;
    }
    .hero .credits .phrase { white-space: nowrap; }
    .hero .credits .sep { color: rgba(110, 118, 129, 0.7); }
    .hero .credits .accent { color: var(--teal); }
    .hero-kraken {
      position: absolute;
      right: clamp(16px, 3vw, 36px);
      bottom: clamp(16px, 3vw, 30px);
      z-index: 2;
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 3px;
      pointer-events: none;
    }
    .hero-kraken .hk-label {
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 2.5px;
      text-transform: uppercase;
      color: rgba(230, 237, 243, 0.6);
      text-shadow: 0 1px 8px rgba(0, 0, 0, 0.9);
    }
    .hero-kraken .hk-name {
      font-family: var(--mono);
      font-weight: 700;
      font-size: clamp(20px, 4vw, 30px);
      line-height: 1;
      letter-spacing: 1px;
      color: var(--teal);
      text-shadow:
        0 0 26px rgba(45, 212, 191, 0.5),
        0 2px 16px rgba(0, 0, 0, 0.85);
    }
    .hero-brand {
      position: absolute;
      top: 20px;
      right: 20px;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background-image: url('/captcrawl.png');
      background-size: cover;
      background-position: center;
      border: 2px solid var(--gold);
      box-shadow:
        0 0 0 1px rgba(0, 0, 0, 0.3),
        0 4px 16px rgba(0, 0, 0, 0.5),
        0 0 24px rgba(249, 192, 21, 0.3);
      z-index: 2;
    }
    @media (max-width: 640px) {
      .hero { height: 240px; }
      .hero h1 { font-size: 32px; gap: 10px; }
      .hero h1 .pirate { font-size: 34px; }
      .hero .tagline { font-size: 14px; margin-bottom: 8px; }
      .hero .credits { font-size: 10px; line-height: 1.6; }
      .hero-content { left: 20px; right: 20px; bottom: 20px; }
      .hero-brand { width: 44px; height: 44px; top: 14px; right: 14px; }
    }
    @media (max-width: 420px) {
      .hero { height: 220px; }
      .hero h1 { font-size: 26px; gap: 8px; margin-bottom: 8px; }
      .hero h1 .pirate { font-size: 28px; }
      .hero .tagline { font-size: 12px; margin-bottom: 6px; }
      .hero .credits { font-size: 9.5px; gap: 2px 5px; }
      .hero-content { left: 16px; right: 16px; bottom: 16px; }
      .hero-brand { width: 38px; height: 38px; top: 12px; right: 12px; border-width: 1.5px; }
    }

    /* ─── Stats row ────────────────────────────────────────────────────── */
    .stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 14px;
      margin-bottom: 24px;
    }
    @media (max-width: 640px) {
      .stats { grid-template-columns: 1fr; }
    }
    .stat {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 18px 20px 20px;
      position: relative;
      overflow: hidden;
      transition: border-color 0.2s, transform 0.2s;
    }
    .stat::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 3px;
      background: var(--accent, var(--border));
    }
    .stat.equity   { --accent: var(--sky); }
    .stat.pnl-pos  { --accent: var(--teal); }
    .stat.pnl-neg  { --accent: var(--coral); }
    .stat.active   { --accent: var(--teal); }
    .stat.paused   { --accent: var(--gold); }
    .stat:hover {
      border-color: var(--border-hi);
      transform: translateY(-1px);
    }
    .stat-label {
      font-size: 10px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 1.2px;
      font-weight: 600;
      margin-bottom: 8px;
    }
    .stat-value {
      font-family: var(--mono);
      font-size: 28px;
      font-weight: 600;
      letter-spacing: -0.5px;
      line-height: 1.1;
    }
    .stat-sub {
      font-size: 11px;
      color: var(--text-dim);
      margin-top: 8px;
      font-family: var(--mono);
    }
    .active-indicator, .paused-indicator {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-family: -apple-system, sans-serif;
      font-size: 22px;
      font-weight: 600;
      letter-spacing: -0.3px;
    }
    .active-indicator { color: var(--teal); }
    .paused-indicator { color: var(--gold); }
    .pulse-dot {
      display: inline-block;
      width: 9px;
      height: 9px;
      border-radius: 50%;
      background: var(--teal);
      box-shadow: 0 0 12px var(--teal);
      animation: pulse 2s ease-in-out infinite;
    }
    .paused-dot {
      display: inline-block;
      width: 9px;
      height: 9px;
      border-radius: 50%;
      background: var(--gold);
      box-shadow: 0 0 12px var(--gold);
    }
    @keyframes pulse {
      0%, 100% { opacity: 1; box-shadow: 0 0 12px var(--teal); }
      50%      { opacity: 0.55; box-shadow: 0 0 20px var(--teal); }
    }

    /* ─── Markets / ticker cards ──────────────────────────────────────── */
    .markets {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 14px;
      margin-bottom: 18px;
    }
    @media (max-width: 920px) {
      .markets { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 600px) {
      .markets { grid-template-columns: 1fr; }
    }
    .ticker {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 18px 20px 16px;
      position: relative;
      overflow: hidden;
      transition: border-color 0.2s, transform 0.2s;
    }
    .ticker::before {
      content: '';
      position: absolute;
      left: 0; top: 0; bottom: 0;
      width: 3px;
      background: var(--accent, var(--border));
    }
    .ticker.t-hbar  { --accent: var(--teal); }
    .ticker.t-btc   { --accent: var(--gold); }
    .ticker.t-dog   { --accent: var(--coral); }
    .ticker.t-sol   { --accent: #9945FF; }
    .ticker.t-sui   { --accent: #4DA2FF; }

    /* ─── Stack Progress section ─────────────────────────────────────── */
    .stack-grid {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .stack-row {
      display: grid;
      grid-template-columns: 60px 1fr 1fr 1fr;
      gap: 14px;
      padding: 14px 16px;
      background: var(--bg-card-hi);
      border: 1px solid var(--border);
      border-radius: 8px;
      align-items: center;
      font-family: var(--mono);
      font-size: 13px;
    }
    @media (max-width: 640px) {
      .stack-row { grid-template-columns: 50px 1fr 1fr; }
      .stack-row .stack-was { display: none; }
    }
    .stack-row .stack-sym strong { font-size: 16px; color: var(--gold); }
    .stack-row .stack-now,
    .stack-row .stack-was,
    .stack-row .stack-delta { line-height: 1.4; }
    .stack-row .stack-delta { text-align: right; font-weight: 600; }
    .stack-row .stack-delta.pos { color: var(--teal); }
    .stack-row .stack-delta.neg { color: var(--coral); }

    /* ─── Capt's Read panel ───────────────────────────────────────────── */
    /* Per-pair structured read from the most recent watch. Stance badge +
       confidence + three-timeframe signal alignment + two factor bullets
       in Capt's voice. Cards are grouped semantically: stack targets
       (HBAR, DOG) on top, vehicles (BTC, SOL, SUI) below — mirroring the
       agent architecture (what we want more of vs. what we use to get it). */
    .reads-stack,
    .reads-vehicles {
      display: grid;
      gap: 14px;
    }
    .reads-stack    { grid-template-columns: repeat(2, 1fr); margin-bottom: 22px; }
    .reads-vehicles { grid-template-columns: repeat(3, 1fr); }
    .reads-divider {
      font-family: var(--mono);
      font-size: 12px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 1.4px;
      font-weight: 600;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .reads-divider::before,
    .reads-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }
    .reads-divider.stack-targets { color: var(--gold); }
    @media (max-width: 920px) {
      .reads-stack    { grid-template-columns: 1fr; }
      .reads-vehicles { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 600px) {
      .reads-vehicles { grid-template-columns: 1fr; }
    }
    .read-card {
      background: var(--bg-card-hi);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 16px 18px 14px;
      position: relative;
      overflow: hidden;
      transition: border-color 0.2s, transform 0.2s;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .read-card::before {
      content: '';
      position: absolute;
      left: 0; top: 0; bottom: 0;
      width: 3px;
      background: var(--accent, var(--border));
    }
    .read-card.t-hbar { --accent: var(--teal); }
    .read-card.t-btc  { --accent: var(--gold); }
    .read-card.t-dog  { --accent: var(--coral); }
    .read-card.t-sol  { --accent: #9945FF; }
    .read-card.t-sui  { --accent: #4DA2FF; }
    .read-card:hover {
      border-color: var(--border-hi);
      transform: translateY(-1px);
    }
    .read-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 8px;
    }
    .read-symbol {
      font-size: 15px;
      font-weight: 700;
      letter-spacing: 0.6px;
      color: var(--text-primary);
    }
    .read-desc {
      font-size: 11.5px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.8px;
      text-align: right;
      flex: 1;
      font-family: var(--mono);
    }
    .read-stance-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
    }
    .stance-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 5px 12px;
      border-radius: 6px;
      font-family: var(--mono);
      font-size: 12.5px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      border: 1px solid transparent;
    }
    .stance-stack {
      color: var(--gold);
      background: rgba(249, 192, 21, 0.08);
      border-color: rgba(249, 192, 21, 0.35);
      box-shadow: 0 0 12px rgba(249, 192, 21, 0.12);
    }
    .stance-buy {
      color: var(--teal);
      background: rgba(45, 212, 191, 0.08);
      border-color: rgba(45, 212, 191, 0.35);
    }
    .stance-hold {
      color: var(--text-muted);
      background: rgba(139, 148, 158, 0.08);
      border-color: rgba(139, 148, 158, 0.28);
    }
    .stance-sell {
      color: var(--coral);
      background: rgba(251, 113, 133, 0.08);
      border-color: rgba(251, 113, 133, 0.35);
    }
    .stance-rotate {
      color: var(--sky);
      background: rgba(97, 166, 221, 0.08);
      border-color: rgba(97, 166, 221, 0.35);
    }
    .stance-watch {
      color: var(--text-dim);
      background: rgba(110, 118, 129, 0.06);
      border-color: rgba(110, 118, 129, 0.25);
    }
    .read-confidence {
      font-family: var(--mono);
      font-size: 12px;
      letter-spacing: 0.4px;
      color: var(--text-muted);
      text-transform: uppercase;
    }
    .read-confidence .conf-value {
      color: var(--text-muted);
      font-weight: 600;
    }
    .read-confidence .conf-value.high { color: var(--text-primary); }
    .read-signals {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 8px;
      padding: 12px 0 10px;
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
    }
    .signal-pill {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 3px;
      font-family: var(--mono);
    }
    .signal-arrow {
      font-size: 15px;
      line-height: 1;
      font-weight: 700;
    }
    .signal-label {
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.3px;
    }
    .signal-tf {
      font-size: 11px;
      color: var(--text-muted);
      letter-spacing: 0.6px;
      text-transform: uppercase;
      margin-top: 2px;
    }
    .signal-bull .signal-arrow,
    .signal-bull .signal-label { color: var(--teal); }
    .signal-neutral .signal-arrow,
    .signal-neutral .signal-label { color: var(--text-muted); }
    .signal-bear .signal-arrow,
    .signal-bear .signal-label { color: var(--coral); }
    .read-factors {
      display: flex;
      flex-direction: column;
      gap: 8px;
      font-family: var(--mono);
      font-size: 13px;
      line-height: 1.5;
      color: var(--text-primary);
      opacity: 0.92;
    }
    .read-factor {
      display: flex;
      gap: 8px;
      align-items: flex-start;
    }
    .read-factor::before {
      content: '·';
      color: var(--gold);
      font-weight: 700;
      flex-shrink: 0;
      width: 6px;
      text-align: center;
    }
    .read-footer {
      font-family: var(--mono);
      font-size: 11.5px;
      color: var(--text-muted);
      letter-spacing: 0.4px;
      text-transform: uppercase;
      margin-top: -2px;
    }
    .reads-empty {
      grid-column: 1 / -1;
    }

    /* ===========================================================================
       Chain Activity — HBAR + DOG on-chain cards.
       Mirrors read-card structure but uses metric-row layout for raw on-chain
       values (TPS, holders, transactions, etc.) with optional baseline deltas.
       =========================================================================== */
    .chain-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 14px;
      margin-top: 4px;
    }
    @media (max-width: 800px) {
      .chain-grid { grid-template-columns: 1fr; }
    }
    .chain-card {
      background: var(--bg-card-hi);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 16px 18px 14px;
      position: relative;
      overflow: hidden;
      transition: border-color 0.2s, transform 0.2s;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }
    .chain-card::before {
      content: '';
      position: absolute;
      left: 0; top: 0; bottom: 0;
      width: 3px;
      background: var(--accent, var(--border));
    }
    .chain-card.t-hbar { --accent: var(--teal); }
    .chain-card.t-dog  { --accent: var(--coral); }
    .chain-card:hover {
      border-color: var(--border-hi);
      transform: translateY(-1px);
    }
    .chain-card.is-down {
      opacity: 0.6;
    }
    .chain-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 8px;
    }
    .chain-symbol {
      font-size: 15px;
      font-weight: 700;
      letter-spacing: 0.6px;
      color: var(--text-primary);
    }
    .chain-source {
      font-size: 11.5px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.8px;
      text-align: right;
      font-family: var(--mono);
    }
    .chain-status {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 3px 8px;
      border-radius: 5px;
      font-family: var(--mono);
      font-size: 10.5px;
      font-weight: 700;
      letter-spacing: 0.8px;
      text-transform: uppercase;
    }
    .chain-status.ok      { background: rgba(45, 212, 191, 0.14); color: var(--teal); }
    .chain-status.partial { background: rgba(248, 196, 113, 0.14); color: #f8c471; }
    .chain-status.failed  { background: rgba(251, 113, 133, 0.14); color: var(--coral); }
    .chain-metrics {
      display: flex;
      flex-direction: column;
      gap: 7px;
    }
    .chain-row {
      display: grid;
      grid-template-columns: 140px 1fr auto;
      gap: 10px;
      align-items: baseline;
      font-family: var(--mono);
      font-size: 13px;
    }
    .chain-row-label {
      color: var(--text-muted);
      font-size: 11.5px;
      letter-spacing: 0.4px;
      text-transform: uppercase;
    }
    .chain-row-value {
      color: var(--text-primary);
      font-weight: 600;
    }
    .chain-row-delta {
      font-size: 11.5px;
      color: var(--text-muted);
      font-family: var(--mono);
      text-align: right;
      white-space: nowrap;
    }
    .chain-row-delta.up   { color: var(--teal); }
    .chain-row-delta.down { color: var(--coral); }
    .chain-empty {
      color: var(--text-muted);
      font-family: var(--mono);
      font-size: 13px;
      padding: 8px 0;
    }
    .chain-footer {
      font-family: var(--mono);
      font-size: 11.5px;
      color: var(--text-muted);
      letter-spacing: 0.4px;
      text-transform: uppercase;
      margin-top: 2px;
    }
    /* Keep the bridge log body readable even on very wide displays — lines
       wider than ~85 chars become harder to track. Caps and centers the inner
       column without affecting the outer card width. */
    .bridge-body {
      max-width: 1000px;
      margin-left: auto;
      margin-right: auto;
    }
    .ticker:hover {
      border-color: var(--border-hi);
      transform: translateY(-1px);
    }
    .ticker-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 8px;
      margin-bottom: 14px;
    }
    .ticker-symbol {
      font-size: 13px;
      font-weight: 700;
      letter-spacing: 0.6px;
      color: var(--text-primary);
    }
    .ticker-quote {
      font-size: 11px;
      color: var(--text-muted);
      font-weight: 500;
      letter-spacing: 0.4px;
    }
    .ticker-desc {
      font-size: 9.5px;
      color: var(--text-dim);
      text-transform: uppercase;
      letter-spacing: 0.8px;
      text-align: right;
      flex: 1;
      font-family: var(--mono);
    }
    .ticker-price {
      font-family: var(--mono);
      font-size: 26px;
      font-weight: 600;
      letter-spacing: -0.5px;
      line-height: 1.1;
      margin-bottom: 10px;
      color: var(--text-primary);
    }
    .ticker-foot {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 8px;
      font-family: var(--mono);
      font-size: 11px;
    }
    .ticker-change {
      font-weight: 600;
    }
    .ticker-range {
      color: var(--text-dim);
      font-size: 10.5px;
    }

    /* ─── Positioning band ────────────────────────────────────────────── */
    /* First thing after the hero. The three-sentence explainer that names
       what this product actually is — a stacking agent for holders with
       conviction, not a generic AI trader. */
    .positioning {
      background:
        linear-gradient(135deg, rgba(45, 212, 191, 0.06) 0%, rgba(249, 192, 21, 0.04) 100%),
        var(--bg-card);
      border: 1px solid var(--border);
      border-left: 3px solid var(--gold);
      border-radius: 10px;
      padding: 22px 28px;
      margin-bottom: 24px;
      font-family: var(--display);
      font-size: 17px;
      line-height: 1.55;
      color: var(--text-primary);
    }
    .positioning .lede {
      color: var(--text-muted);
    }
    .positioning .accent {
      color: var(--gold);
      font-weight: 500;
    }
    .positioning .signature {
      display: block;
      margin-top: 14px;
      font-family: var(--mono);
      font-size: 11px;
      font-style: normal;
      color: var(--teal);
      letter-spacing: 0.8px;
      text-transform: uppercase;
    }
    @media (max-width: 640px) {
      .positioning { padding: 18px 20px; font-size: 15px; }
    }

    /* ─── Section sub-callout ─────────────────────────────────────────── */
    /* Used under a section header to give context on what the user is
       looking at — "Token quantity over dollar PnL" etc. */
    .section-callout {
      font-family: var(--mono);
      font-size: 13.5px;
      color: var(--text-muted);
      letter-spacing: 0.2px;
      margin: -8px 0 20px;
      padding-bottom: 14px;
      border-bottom: 1px dashed var(--border);
      font-style: italic;
      line-height: 1.55;
    }

    /* ─── From the Bridge — latest Bridge Log on the dashboard ────────── */
    /* Capt's voice as a first-class dashboard section, not Discord-only.
       Styled to feel like opening a captain's logbook — gold side accent,
       mono body, the 🏴‍☠️ sign-off centered, attribution in faded type. */
    #bridge-section { padding-bottom: 8px; }
    .bridge-wrap {
      background: var(--bg-card-hi);
      border: 1px solid var(--border);
      border-left: 3px solid var(--gold);
      border-radius: 8px;
      padding: 22px 26px 20px;
      position: relative;
    }
    .bridge-meta {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 18px;
      padding-bottom: 12px;
      border-bottom: 1px solid var(--border);
    }
    .bridge-meta .bridge-stamp {
      font-family: var(--mono);
      font-size: 12.5px;
      color: var(--gold);
      letter-spacing: 0.8px;
      text-transform: uppercase;
      font-weight: 600;
    }
    .bridge-meta .bridge-run {
      font-family: var(--mono);
      font-size: 11.5px;
      color: var(--text-muted);
      letter-spacing: 0.4px;
    }
    .bridge-body {
      font-family: var(--mono);
      font-size: 14.5px;
      line-height: 1.75;
      color: var(--text-primary);
    }
    .bridge-body p {
      margin: 0 0 16px;
    }
    .bridge-body p:last-child { margin-bottom: 0; }
    .bridge-flag {
      text-align: center;
      font-size: 24px;
      margin: 18px 0 10px;
      filter: drop-shadow(0 0 12px rgba(249, 192, 21, 0.35));
    }
    .bridge-attribution {
      text-align: center;
      font-family: var(--mono);
      font-size: 11px;
      color: var(--text-muted);
      letter-spacing: 0.8px;
      line-height: 1.6;
    }
    .bridge-attribution .by {
      color: var(--text-primary);
      font-weight: 600;
    }
    .bridge-attribution .b4e {
      display: block;
      color: var(--teal);
      font-size: 10px;
      margin-top: 2px;
      letter-spacing: 1.2px;
    }
    @media (max-width: 640px) {
      .bridge-wrap { padding: 18px 18px 16px; }
      .bridge-body { font-size: 12.5px; line-height: 1.65; }
    }

    /* ─── Markets compact strip ───────────────────────────────────────── */
    /* Demoted from a 5-card grid (which duplicated Capt's Read coverage) to
       a tighter horizontal strip. Same per-pair data, less visual weight. */
    .markets-strip {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 10px;
    }
    @media (max-width: 920px) {
      .markets-strip { grid-template-columns: repeat(3, 1fr); }
    }
    @media (max-width: 600px) {
      .markets-strip { grid-template-columns: repeat(2, 1fr); }
    }
    .market-mini {
      background: var(--bg-card-hi);
      border: 1px solid var(--border);
      border-left: 2px solid var(--accent, var(--border));
      border-radius: 6px;
      padding: 10px 12px 9px;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }
    .market-mini.t-hbar { --accent: var(--teal); }
    .market-mini.t-btc  { --accent: var(--gold); }
    .market-mini.t-dog  { --accent: var(--coral); }
    .market-mini.t-sol  { --accent: #9945FF; }
    .market-mini.t-sui  { --accent: #4DA2FF; }
    .market-mini .mm-sym {
      font-size: 10.5px;
      font-weight: 700;
      letter-spacing: 0.5px;
      color: var(--text-primary);
    }
    .market-mini .mm-price {
      font-family: var(--mono);
      font-size: 14px;
      font-weight: 600;
      color: var(--text-primary);
      letter-spacing: -0.2px;
    }
    .market-mini .mm-change {
      font-family: var(--mono);
      font-size: 10.5px;
      font-weight: 600;
    }
    .market-mini .mm-change.pos { color: var(--teal); }
    .market-mini .mm-change.neg { color: var(--coral); }

    /* ─── Section ──────────────────────────────────────────────────────── */
    section {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 22px 24px 24px;
      margin-bottom: 18px;
      transition: border-color 0.2s;
    }
    section:hover { border-color: var(--border-hi); }
    section h2 {
      font-size: 14px;
      font-weight: 700;
      color: var(--gold);
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: 18px;
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      padding-bottom: 12px;
      border-bottom: 1px solid var(--border);
    }
    section h2 .count {
      color: var(--text-dim);
      font-weight: 400;
      font-size: 11px;
      letter-spacing: 0.5px;
      font-family: var(--mono);
    }

    /* ─── The Reckoning — Captain's Logbook ────────────────── */
    .reck-sub { color: var(--text-muted); font-size: 12px; margin: -8px 0 16px; font-style: italic; }
    .reck-assets { display: grid; grid-template-columns: repeat(auto-fill, minmax(155px, 1fr)); gap: 12px; margin-bottom: 18px; }
    .reck-card { background: var(--bg-card-hi); border: 1px solid var(--border); border-radius: 8px; padding: 12px 14px; position: relative; overflow: hidden; }
    .reck-card::before { content: ''; position: absolute; left: 0; top: 0; bottom: 0; width: 3px; background: var(--border); }
    .reck-card.up::before { background: var(--teal); }
    .reck-card.down::before { background: var(--coral); }
    .reck-card-pair { font-family: var(--mono); font-weight: 700; font-size: 13px; letter-spacing: 0.5px; }
    .reck-card-rec { font-family: var(--mono); font-size: 12px; margin-top: 4px; }
    .reck-card-net { font-family: var(--mono); font-weight: 700; font-size: 16px; margin-top: 6px; }
    .reck-card-held { font-family: var(--mono); font-size: 10.5px; margin-top: 2px; }
    .reck-log { display: flex; flex-direction: column; gap: 6px; }
    .reck-row { display: grid; grid-template-columns: 24px 64px 1fr auto auto auto; gap: 10px; align-items: center; padding: 8px 11px; background: var(--bg-card-hi); border: 1px solid var(--border); border-radius: 6px; font-size: 12px; font-family: var(--mono); }
    .reck-row .reck-verdict { font-weight: 700; text-align: center; font-size: 13px; }
    .reck-correct .reck-verdict { color: var(--teal); }
    .reck-wrong .reck-verdict { color: var(--coral); }
    .reck-neutral .reck-verdict { color: var(--text-dim); }
    .reck-row .reck-tok { text-align: right; }
    @media (max-width: 640px) {
      .reck-row { grid-template-columns: 22px 1fr auto auto; }
      .reck-row .reck-px, .reck-row .reck-reason { display: none; }
    }

    /* ─── Equity chart ────────────────────────────────────────────────── */
    .chart-wrap { width: 100%; position: relative; }
    .chart svg { display: block; width: 100%; height: 240px; }
    .chart-empty {
      color: var(--text-muted);
      padding: 50px 20px;
      text-align: center;
      font-style: italic;
      font-size: 13px;
      line-height: 1.7;
      background:
        repeating-linear-gradient(45deg,
          transparent 0, transparent 8px,
          rgba(45, 212, 191, 0.03) 8px, rgba(45, 212, 191, 0.03) 9px);
      border-radius: 8px;
      border: 1px dashed var(--border);
    }
    .chart-empty strong { color: var(--teal); font-style: normal; }
    .chart-line {
      fill: none;
      stroke: var(--teal);
      stroke-width: 2.5;
      stroke-linejoin: round;
      stroke-linecap: round;
      filter: drop-shadow(0 0 6px var(--teal-glow));
    }
    .chart-fill { fill: url(#equityGradient); opacity: 0.6; }
    .chart-baseline {
      fill: none;
      stroke: var(--text-dim);
      stroke-width: 1;
      stroke-dasharray: 4 6;
      opacity: 0.6;
    }
    .chart-axis { stroke: var(--border); stroke-width: 1; }
    .chart-label { fill: var(--text-muted); font-family: var(--mono); font-size: 10px; }
    .chart-dot { fill: var(--teal); filter: drop-shadow(0 0 4px var(--teal)); }

    /* ─── The Chart — price + watched levels + fills ───────────────────── */
    .tchart-controls { display: flex; flex-wrap: wrap; gap: 12px; align-items: center; margin-bottom: 14px; }
    .seg { display: inline-flex; gap: 4px; background: var(--bg-deep); border: 1px solid var(--border); border-radius: 8px; padding: 3px; }
    .seg-btn {
      font-family: var(--mono); font-size: 11px; letter-spacing: 0.5px;
      color: var(--text-muted); background: transparent; border: none; cursor: pointer;
      padding: 5px 11px; border-radius: 6px; transition: background 0.15s, color 0.15s;
    }
    .seg-btn:hover { color: var(--text-primary); }
    .seg-btn.active { background: var(--bg-card-hi); color: var(--gold); box-shadow: inset 0 0 0 1px var(--border-hi); }
    #thesis-chart { width: 100%; height: 380px; position: relative; }
    .tchart-tip {
      position: absolute; z-index: 5; pointer-events: none;
      background: var(--bg-card-hi); border: 1px solid var(--border-hi);
      border-radius: 8px; padding: 8px 11px; font-family: var(--mono); font-size: 11px;
      color: var(--text-primary); box-shadow: 0 6px 20px rgba(0,0,0,0.5); max-width: 250px;
    }
    .tchart-tip .tip-h { color: var(--gold); font-size: 10.5px; letter-spacing: 0.3px; margin-bottom: 4px; }
    .tchart-tip > div { line-height: 1.55; }
    .tchart-tip .dim { color: var(--text-dim); }
    .tchart-tip .pos { color: var(--teal); }
    .tchart-tip .neg { color: var(--coral); }
    .tchart-legend { display: flex; flex-wrap: wrap; gap: 16px; margin-top: 14px; font-family: var(--mono); font-size: 10.5px; color: var(--text-dim); }
    .tchart-legend .lg { display: inline-flex; align-items: center; gap: 6px; }
    .tchart-legend .sw { width: 16px; border-top: 2px solid currentColor; display: inline-block; }
    .tchart-legend .sw.dash { border-top-style: dotted; }
    .tchart-legend .acc { color: var(--teal); }
    .tchart-legend .trim { color: var(--coral); }
    .tchart-legend .buy { color: var(--sky); }
    .tchart-legend .win { color: var(--teal); }
    .tchart-legend .loss { color: var(--coral); }

    /* ─── Position cards ─────────────────────────────────────────────── */
    .position {
      border: 1px solid var(--border);
      border-left: 3px solid var(--accent, var(--border));
      border-radius: 8px;
      padding: 16px 18px;
      margin-bottom: 12px;
      background: var(--bg-deep);
      transition: background 0.2s, transform 0.2s;
    }
    .position.tier-rail     { --accent: var(--sky); }
    .position.tier-one_out  { --accent: var(--teal); }
    .position.tier-two_out  { --accent: var(--coral); }
    .position.forced        { --accent: var(--gold); }
    .position:last-child    { margin-bottom: 0; }
    .position:hover {
      background: var(--bg-card-hi);
      transform: translateX(2px);
    }
    .position-head {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 14px;
    }
    .position-pair {
      font-size: 18px;
      font-weight: 700;
      letter-spacing: -0.2px;
      color: var(--text-primary);
    }
    .position-tag {
      font-size: 10px;
      padding: 3px 8px;
      border-radius: 4px;
      text-transform: uppercase;
      letter-spacing: 0.6px;
      font-weight: 700;
      margin-left: 10px;
      vertical-align: middle;
    }
    .tag-rail     { background: rgba(97, 166, 221, 0.15); color: var(--sky); }
    .tag-one_out  { background: rgba(45, 212, 191, 0.15); color: var(--teal); }
    .tag-two_out  { background: rgba(251, 113, 133, 0.15); color: var(--coral); }
    .tag-forced   { background: rgba(249, 192, 21, 0.15); color: var(--gold); }
    .position-row {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 14px;
      font-family: var(--mono);
    }
    .position-cell .k {
      display: block;
      font-size: 9.5px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.8px;
      margin-bottom: 4px;
      font-family: -apple-system, sans-serif;
      font-weight: 600;
    }
    .position-cell .v { font-size: 13px; font-weight: 500; }
    .position-cell .v2 {
      display: block;
      font-size: 10.5px;
      color: var(--text-muted);
      margin-top: 4px;
      font-family: var(--mono);
      font-weight: 400;
    }
    .position-cell .v2.dim { color: var(--text-dim); }

    .empty-state {
      color: var(--text-muted);
      padding: 32px 20px;
      text-align: center;
      font-size: 13px;
      line-height: 1.7;
    }
    .empty-state strong { color: var(--gold); font-weight: 600; }
    .empty-state .quote {
      font-style: italic;
      color: var(--text-dim);
      font-size: 12px;
      margin-top: 8px;
    }

    /* ─── Closes ──────────────────────────────────────────────────────── */
    .close-row {
      display: grid;
      grid-template-columns: 50px 1fr 110px 130px 120px;
      gap: 10px;
      align-items: center;
      padding: 12px 0;
      border-bottom: 1px solid var(--border);
      font-family: var(--mono);
      font-size: 13px;
    }
    .close-row:last-child { border-bottom: none; }
    .reason-chip {
      font-family: -apple-system, sans-serif;
      font-size: 10px;
      padding: 4px 10px;
      border-radius: 4px;
      text-transform: uppercase;
      letter-spacing: 0.6px;
      font-weight: 700;
      text-align: center;
      display: inline-block;
    }
    .reason-invalidation { background: rgba(251, 113, 133, 0.18); color: var(--coral); }
    .reason-take_profit  { background: rgba(45, 212, 191, 0.18); color: var(--teal); }
    .reason-time_stop    { background: rgba(249, 192, 21, 0.18); color: var(--gold); }
    .reason-plank_walk   { background: rgba(230, 0, 0, 0.18);    color: var(--rebel-red); }
    .reason-manual       { background: rgba(97, 166, 221, 0.18); color: var(--sky); }
    .reason-rotation     { background: rgba(139, 148, 158, 0.18); color: var(--text-muted); }

    /* ─── Decisions ───────────────────────────────────────────────────── */
    .decision {
      border-bottom: 1px solid var(--border);
      padding: 16px 0;
    }
    .decision:last-child { border-bottom: none; }
    .decision:first-child { padding-top: 4px; }
    .decision-head {
      display: flex;
      align-items: baseline;
      gap: 10px;
      flex-wrap: wrap;
      margin-bottom: 10px;
    }
    .decision-runid {
      font-family: var(--mono);
      color: var(--text-muted);
      font-size: 12px;
      font-weight: 600;
    }
    .decision-action {
      font-weight: 700;
      font-size: 11px;
      padding: 3px 10px;
      border-radius: 4px;
      text-transform: uppercase;
      letter-spacing: 0.6px;
    }
    .action-enter { background: rgba(45, 212, 191, 0.18); color: var(--teal); }
    .action-hold  { background: rgba(139, 148, 158, 0.15); color: var(--text-muted); }
    .decision-type {
      font-family: var(--mono);
      font-size: 11px;
      color: var(--text-dim);
      padding: 2px 8px;
      background: var(--bg-deep);
      border-radius: 4px;
    }
    .decision-time {
      margin-left: auto;
      font-size: 11px;
      color: var(--text-dim);
      font-family: var(--mono);
    }
    .decision-thesis {
      font-size: 13.5px;
      color: var(--text-primary);
      line-height: 1.75;
      margin-top: 8px;
      white-space: pre-wrap;
      padding-left: 12px;
      border-left: 2px solid var(--border);
    }
    .decision-confidence {
      font-size: 11px;
      color: var(--text-muted);
      margin-top: 8px;
      font-family: var(--mono);
      letter-spacing: 0.3px;
    }

    /* ─── Plank walks ─────────────────────────────────────────────────── */
    .plank-walk {
      border: 1px solid rgba(230, 0, 0, 0.4);
      border-left: 3px solid var(--rebel-red);
      border-radius: 6px;
      padding: 14px 16px;
      margin-bottom: 10px;
      background: rgba(230, 0, 0, 0.04);
    }
    .plank-walk-head {
      font-size: 14px;
      font-weight: 700;
      color: var(--rebel-red);
      margin-bottom: 6px;
      letter-spacing: -0.2px;
    }
    .plank-walk-detail {
      font-size: 12px;
      color: var(--text-muted);
      font-family: var(--mono);
    }

    /* ─── Footer ──────────────────────────────────────────────────────── */
    footer {
      margin-top: 36px;
      padding-top: 24px;
      border-top: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 16px;
    }
    @media (max-width: 640px) {
      footer { flex-direction: column; text-align: center; }
    }
    .footer-brand {
      display: flex;
      align-items: center;
      gap: 14px;
    }
    .footer-brand img {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      border: 1.5px solid var(--gold);
      background: var(--gold);
    }
    .footer-brand-text {
      font-family: var(--mono);
      font-size: 11px;
      color: var(--text-dim);
      letter-spacing: 0.6px;
      line-height: 1.6;
    }
    .footer-brand-text strong {
      color: var(--gold);
      font-weight: 600;
      letter-spacing: 1px;
    }
    .footer-status {
      text-align: right;
      color: var(--text-dim);
      font-size: 11px;
      font-family: var(--mono);
      letter-spacing: 0.4px;
    }
    .footer-status .heart {
      color: var(--coral);
      animation: pulse 2s ease-in-out infinite;
      display: inline-block;
    }
    .footer-status .signoff {
      margin-top: 4px;
      color: var(--text-dim);
      letter-spacing: 1px;
    }
  </style>
</head>
<body>
  <div class="wrap">
    <!-- Hero banner with plank-hero image -->
    <header class="hero">
      <div class="hero-image"></div>
      <div class="hero-overlay"></div>
      <div class="hero-brand"></div>
      <div class="hero-content">
        <h1><span class="pirate">🏴‍☠️</span> Capt's Ledger</h1>
        <div class="tagline">A stacking agent for holders with conviction.</div>
        <div class="credits">
          <span class="phrase">The Watch</span>
          <span class="sep">·</span>
          <span class="phrase">By Capt. Crawl</span>
          <span class="sep">·</span>
          <span class="phrase"><a class="accent" href="https://x.com/Boons4ever" target="_blank" rel="noopener">@Boons4ever</a></span>
          <span class="sep">·</span>
          <span class="phrase accent">B4E</span>
        </div>
      </div>
      <div class="hero-kraken">
        <span class="hk-label">Built on</span>
        <span class="hk-name">Kraken CLI</span>
      </div>
    </header>

    <!-- Positioning band — what this product actually is -->
    <div class="positioning">
      <span class="lede">Most trading agents try to make you dollars.</span>
      <span class="accent">The Watch tries to make you more of the tokens you actually want to hold long-term.</span>
      For holders, not flippers.
      <span class="signature">— The Watch · A stacking agent for the Boons</span>
    </div>

    <!-- Live account stats -->
    <div class="stats">
      <div class="stat equity" id="stat-equity-card">
        <div class="stat-label">Account Equity</div>
        <div class="stat-value mono" id="stat-equity">—</div>
        <div class="stat-sub" id="stat-equity-sub">&nbsp;</div>
      </div>
      <div class="stat" id="stat-pnl-card">
        <div class="stat-label">Session P&L</div>
        <div class="stat-value mono" id="stat-pnl">—</div>
        <div class="stat-sub" id="stat-pnl-sub">&nbsp;</div>
      </div>
      <div class="stat" id="stat-scheduler-card">
        <div class="stat-label">Scheduler</div>
        <div class="stat-value" id="stat-scheduler">—</div>
        <div class="stat-sub" id="stat-scheduler-sub">&nbsp;</div>
      </div>
    </div>

    <!-- Stack Progress — the differentiated metric, surfaced near the top -->
    <section id="stack-section">
      <h2>Stack Progress <span class="count" id="stack-status">—</span></h2>
      <div class="section-callout">
        Token quantity over dollar PnL. The scoreboard is HBAR + DOG count vs. baseline — not USD. A rotation that ends with more HBAR is a win even if the dollar number didn't move.
      </div>
      <div id="stack"></div>
    </section>

    <!-- From the Bridge — Capt's voice on the dashboard, not Discord-only -->
    <section id="bridge-section">
      <h2>From the Bridge <span class="count" id="bridge-status">—</span></h2>
      <div class="section-callout">
        Capt's most recent log. Filed every watch — twice daily on schedule, plus exit-checks and manual fires.
      </div>
      <div id="bridge"></div>
    </section>

    <!-- Capt's Read — per-pair structured stance, signals, factor bullets -->
    <section id="reasoning-section">
      <h2>Capt's Read <span class="count" id="reasoning-status">—</span></h2>
      <div class="section-callout">
        Where Capt stands on each pair this watch. Stance, three-timeframe signal alignment, and two short observation bullets per pair.
      </div>
      <div id="reasoning"></div>
    </section>

    <!-- Chain Activity — on-chain data feeds for HBAR + DOG stacking targets -->
    <section id="onchain-section">
      <h2>Chain Activity <span class="count" id="onchain-status">—</span></h2>
      <div class="section-callout">
        On-chain context the price chart can't show. Hedera Mirror Node for HBAR network activity, Unisat for DOG runes-marketplace data. Capt sees this alongside the tape every watch.
      </div>
      <div id="onchain"></div>
    </section>

    <!-- Markets — compact reference strip showing current prices across all five pairs -->
    <section id="markets-section">
      <h2>Markets <span class="count" id="markets-count">—</span></h2>
      <div id="markets"></div>
    </section>

    <!-- The Chart — price candles + Capt's watched levels + fills (Kraken-CLI spine) -->
    <section id="thesis-chart-section">
      <h2>The Chart <span class="count" id="thesis-chart-status">—</span></h2>
      <div class="tchart-controls">
        <div class="seg" id="thesis-pairs"></div>
        <div class="seg" id="thesis-tfs"></div>
      </div>
      <div class="chart-wrap"><div id="thesis-chart"></div></div>
      <div class="tchart-legend">
        <span class="lg acc"><span class="sw"></span> stack level (watching)</span>
        <span class="lg trim"><span class="sw"></span> trim level</span>
        <span class="lg dim"><span class="sw dash"></span> superseded</span>
        <span class="lg buy">▲ entry</span>
        <span class="lg win">▼ exit · win</span>
        <span class="lg loss">▼ exit · loss</span>
      </div>
    </section>

    <section>
      <h2>Equity Curve <span class="count" id="equity-count">—</span></h2>
      <div class="chart-wrap chart" id="chart"></div>
    </section>

    <!-- The Reckoning — Captain's Logbook: every read scored and recorded -->
    <section id="reckoning-section">
      <h2>The Reckoning <span class="count" id="reckoning-count">—</span></h2>
      <div class="reck-sub">Captain's Logbook · every call scored, win or wrong</div>
      <div class="reck-assets" id="reck-assets"></div>
      <div class="reck-log" id="reck-log"></div>
    </section>

    <section>
      <h2>Open Positions <span class="count" id="positions-count">—</span></h2>
      <div id="positions"></div>
    </section>

    <section>
      <h2>Recent Decisions <span class="count" id="decisions-count">—</span></h2>
      <div id="decisions"></div>
    </section>

    <section>
      <h2>Recent Closes <span class="count" id="closes-count">—</span></h2>
      <div id="closes"></div>
    </section>

    <section id="plank-walks-section" style="display: none;">
      <h2>Plank Walks <span class="count" id="plank-walks-count">—</span></h2>
      <div id="plank-walks"></div>
    </section>

    <footer>
      <div class="footer-brand">
        <img src="/captcrawl.png" alt="Capt. Crawl" />
        <div class="footer-brand-text">
          <strong>CAPT. CRAWL</strong><br>
          The Watch · B4E · 4ever
        </div>
      </div>
      <div class="footer-status">
        <div class="refresh-status" id="refresh-status">connecting...</div>
        <div class="signoff">🏴‍☠️ built on kraken cli</div>
      </div>
    </footer>
  </div>

  <script src="/lightweight-charts.standalone.production.js"></script>
  <script>
    const fmtUsd = (v) => {
      if (v === null || v === undefined) return '—';
      const sign = v < 0 ? '-' : '';
      return sign + '$' + Math.abs(v).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    };
    const fmtUsdSigned = (v) => {
      if (v === null || v === undefined) return '—';
      const sign = v >= 0 ? '+' : '-';
      return sign + '$' + Math.abs(v).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    };
    const fmtPct = (v) => {
      if (v === null || v === undefined) return '—';
      const sign = v >= 0 ? '+' : '';
      return sign + v.toFixed(2) + '%';
    };
    const fmtPrice = (v) => {
      if (v === null || v === undefined) return '—';
      if (v >= 100) return '$' + v.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
      if (v >= 1)   return '$' + v.toFixed(4);
      return '$' + v.toFixed(6);
    };
    const fmtVolume = (v) => {
      if (v === null || v === undefined) return '—';
      if (v >= 1)    return v.toLocaleString('en-US', { maximumFractionDigits: 4 });
      if (v >= 0.01) return v.toFixed(4);
      return v.toFixed(8);
    };
    const fmtCtTime = (iso) => {
      try {
        return new Date(iso).toLocaleString('en-US', {
          timeZone: 'America/Chicago',
          month: 'short', day: 'numeric',
          hour: '2-digit', minute: '2-digit',
          hour12: true,
        }) + ' CT';
      } catch { return '—'; }
    };
    const fmtHours = (h) => {
      if (h === null || h === undefined || h <= 0) return '0h 0m';
      const hours = Math.floor(h);
      const mins = Math.round((h - hours) * 60);
      return hours + 'h ' + mins + 'm';
    };
    const pnlClass = (v) => v >= 0 ? 'pos' : 'neg';

    function renderState(state) {
      const account = state.account;
      const equityEl = document.getElementById('stat-equity');
      const equitySubEl = document.getElementById('stat-equity-sub');
      const pnlEl = document.getElementById('stat-pnl');
      const pnlSubEl = document.getElementById('stat-pnl-sub');
      const pnlCardEl = document.getElementById('stat-pnl-card');
      const schedEl = document.getElementById('stat-scheduler');
      const schedSubEl = document.getElementById('stat-scheduler-sub');
      const schedCardEl = document.getElementById('stat-scheduler-card');

      if (account) {
        equityEl.textContent = fmtUsd(account.current_value);
        equitySubEl.textContent = '— from $' + account.starting_balance.toLocaleString('en-US', { minimumFractionDigits: 2 }) + ' baseline';
        pnlEl.textContent = fmtPct(account.pnl_pct);
        pnlEl.className = 'stat-value mono ' + pnlClass(account.pnl_pct);
        pnlCardEl.className = 'stat ' + (account.pnl_pct >= 0 ? 'pnl-pos' : 'pnl-neg');
        // Surface the dollar P&L alongside the trade count. Showing both the
        // percent (big) and dollar (sub) makes the card honest at a glance —
        // a 0.5% drawdown is very different from a 50% drawdown, and the
        // dollar number is the one that fits in your head.
        const pnlUsdStr = account.pnl_usd !== undefined && account.pnl_usd !== null
          ? fmtUsdSigned(account.pnl_usd) + ' · '
          : '';
        pnlSubEl.textContent = pnlUsdStr + account.total_trades + ' total trades this session';
      } else {
        equityEl.textContent = '—';
        pnlEl.textContent = '—';
        equitySubEl.textContent = 'kraken cli unreachable';
        pnlSubEl.textContent = '';
      }
      if (state.paused) {
        schedEl.innerHTML = '<span class="paused-indicator"><span class="paused-dot"></span>PAUSED</span>';
        schedSubEl.textContent = 'scheduled fires skipped · manual fires still work';
        schedCardEl.className = 'stat paused';
      } else {
        schedEl.innerHTML = '<span class="active-indicator"><span class="pulse-dot"></span>ACTIVE</span>';
        schedSubEl.textContent = '9am / 9pm CT bridges · 3am / 3pm CT exit-checks';
        schedCardEl.className = 'stat active';
      }
      renderPositions(state.positions || []);
    }

    const PAIR_META = {
      HBARUSD:  { symbol: 'HBAR',  desc: 'HBARBARIANS · STACK TARGET',  cls: 't-hbar' },
      BTCUSD:   { symbol: 'BTC',   desc: 'THE RESERVE · VEHICLE',       cls: 't-btc' },
      DOGUSD:   { symbol: 'DOG',   desc: 'DOG ARMY · STACK TARGET',     cls: 't-dog' },
      SOLUSD:   { symbol: 'SOL',   desc: 'SOLANA · VEHICLE',            cls: 't-sol' },
      SUIUSD:   { symbol: 'SUI',   desc: 'SUI · VEHICLE',               cls: 't-sui' },
    };

    function renderTickers(tickers) {
      const container = document.getElementById('markets');
      const countEl   = document.getElementById('markets-count');
      if (!tickers || tickers.length === 0) {
        container.innerHTML = '';
        if (countEl) countEl.textContent = '—';
        return;
      }
      if (countEl) countEl.textContent = '(' + tickers.length + ' pair' + (tickers.length === 1 ? '' : 's') + ')';
      container.innerHTML = '<div class="markets-strip">' + tickers.map(t => {
        const meta = PAIR_META[t.pair] || { symbol: t.pair, desc: '', cls: '' };
        const changeHtml = t.pct_change_24h !== null
          ? '<span class="mm-change ' + pnlClass(t.pct_change_24h) + '">' + fmtPct(t.pct_change_24h) + '</span>'
          : '<span class="mm-change dim">—</span>';
        return `
          <div class="market-mini ${meta.cls}">
            <span class="mm-sym">${meta.symbol}</span>
            <span class="mm-price">${fmtPrice(t.last)}</span>
            ${changeHtml}
          </div>`;
      }).join('') + '</div>';
    }

    function renderPositions(positions) {
      const container = document.getElementById('positions');
      document.getElementById('positions-count').textContent = '(' + positions.length + ')';
      if (positions.length === 0) {
        container.innerHTML = `
          <div class="empty-state">
            <strong>Marker on the deck.</strong><br>
            No positions held — Capt is reading the books.
            <div class="quote">"The discipline is not manufacturing signal where there isn't any."</div>
          </div>`;
        return;
      }
      container.innerHTML = positions.map(p => {
        const cardClass = p.forced ? 'forced' : ('tier-' + p.size_label);
        const tag = p.forced
          ? '<span class="position-tag tag-forced">FORCED</span>'
          : '<span class="position-tag tag-' + p.size_label + '">' + p.size_label + '</span>';
        const pnlUsdHtml = p.unrealized_pnl_usd !== null
          ? '<span class="' + pnlClass(p.unrealized_pnl_usd) + '">' + fmtUsdSigned(p.unrealized_pnl_usd) + '</span>'
          : '—';
        const pnlPctHtml = p.unrealized_pnl_pct !== null
          ? '<span class="' + pnlClass(p.unrealized_pnl_pct) + '">' + fmtPct(p.unrealized_pnl_pct) + '</span>'
          : '—';
        const valueHtml = p.current_value_usd !== null ? fmtUsd(p.current_value_usd) : '—';
        return `
          <div class="position ${cardClass}">
            <div class="position-head">
              <div>
                <span class="position-pair">${p.pair}</span>
                ${tag}
              </div>
              <div class="muted mono" style="font-size: 11px;">opened ${fmtCtTime(p.opened_at)}</div>
            </div>
            <div class="position-row">
              <div class="position-cell">
                <span class="k">Holding</span>
                <span class="v">${fmtVolume(p.volume)} ${p.symbol}</span>
                <span class="v2">${fmtUsd(p.cost_usd)} @ ${fmtPrice(p.fill_price)} entry</span>
              </div>
              <div class="position-cell">
                <span class="k">Current Value</span>
                <span class="v">${valueHtml}</span>
                <span class="v2">@ ${fmtPrice(p.current_price)} mark</span>
              </div>
              <div class="position-cell">
                <span class="k">Unrealized</span>
                <span class="v">${pnlUsdHtml}</span>
                <span class="v2">${pnlPctHtml}</span>
              </div>
              <div class="position-cell">
                <span class="k">Invalidation</span>
                <span class="v">${p.invalidation_price !== null ? fmtPrice(p.invalidation_price) : '—'}</span>
                <span class="v2 dim">loss exit</span>
              </div>
              <div class="position-cell">
                <span class="k">Take-Profit</span>
                <span class="v">${p.take_profit_price !== null ? fmtPrice(p.take_profit_price) : '—'}</span>
                <span class="v2 dim">win exit</span>
              </div>
              <div class="position-cell">
                <span class="k">Time-Stop</span>
                <span class="v">${fmtHours(p.hours_to_time_stop)}</span>
                <span class="v2 dim">forced exit</span>
              </div>
            </div>
          </div>`;
      }).join('');
    }

    function renderEquity(snapshots) {
      const container = document.getElementById('chart');
      document.getElementById('equity-count').textContent = '(' + snapshots.length + ' snapshot' + (snapshots.length === 1 ? '' : 's') + ')';
      if (snapshots.length === 0) {
        container.innerHTML = `
          <div class="chart-empty">
            <strong>Awaiting first snapshot.</strong><br>
            The curve will fill in as the watch runs: <br>
            two snapshots per day from each Bridge Log, two more from each Exit-Check.
          </div>`;
        return;
      }
      const W = 900, H = 240;
      const padL = 56, padR = 14, padT = 18, padB = 30;
      const innerW = W - padL - padR;
      const innerH = H - padT - padB;
      const xs = snapshots.map(s => new Date(s.ts).getTime());
      const ys = snapshots.map(s => s.value);
      const starting = snapshots[0].starting || 10000;
      const xMin = Math.min(...xs);
      const xMax = Math.max(...xs);
      const xRange = Math.max(1, xMax - xMin);
      const yMinRaw = Math.min(...ys, starting);
      const yMaxRaw = Math.max(...ys, starting);
      const yPad = (yMaxRaw - yMinRaw) * 0.15 || 100;
      const yMin = yMinRaw - yPad;
      const yMax = yMaxRaw + yPad;
      const yRange = Math.max(1, yMax - yMin);
      const sx = (x) => padL + ((x - xMin) / xRange) * innerW;
      const sy = (y) => padT + (1 - (y - yMin) / yRange) * innerH;
      const pts = snapshots.map(s => ({ x: sx(new Date(s.ts).getTime()), y: sy(s.value) }));
      const linePoints = pts.map(p => `${p.x.toFixed(1)},${p.y.toFixed(1)}`).join(' ');
      const fillPath = `M ${pts[0].x.toFixed(1)},${(padT + innerH).toFixed(1)} ` +
        pts.map(p => `L ${p.x.toFixed(1)},${p.y.toFixed(1)}`).join(' ') +
        ` L ${pts[pts.length - 1].x.toFixed(1)},${(padT + innerH).toFixed(1)} Z`;
      const yTicks = 4;
      const yLabels = [];
      for (let i = 0; i <= yTicks; i++) {
        const v = yMin + (yRange / yTicks) * i;
        const y = sy(v);
        yLabels.push(`<text class="chart-label" x="${padL - 8}" y="${y + 3}" text-anchor="end">$${Math.round(v).toLocaleString('en-US')}</text>`);
        yLabels.push(`<line class="chart-axis" x1="${padL}" y1="${y}" x2="${W - padR}" y2="${y}" opacity="0.25" />`);
      }
      const xLabels = [];
      [0, 0.5, 1].forEach(frac => {
        const ts = xMin + xRange * frac;
        const x = padL + frac * innerW;
        const label = new Date(ts).toLocaleString('en-US', { timeZone: 'America/Chicago', month: 'short', day: 'numeric', hour: 'numeric', hour12: true });
        xLabels.push(`<text class="chart-label" x="${x}" y="${H - padB + 16}" text-anchor="middle">${label}</text>`);
      });
      const baseY = sy(starting);
      const baselineEl = `<line class="chart-baseline" x1="${padL}" y1="${baseY}" x2="${W - padR}" y2="${baseY}" />
                          <text class="chart-label" x="${W - padR - 6}" y="${baseY - 5}" text-anchor="end" style="fill: var(--text-dim);">$${starting.toLocaleString('en-US')} baseline</text>`;
      const dots = pts.map(p => `<circle class="chart-dot" cx="${p.x.toFixed(1)}" cy="${p.y.toFixed(1)}" r="3.5" />`).join('');
      container.innerHTML = `
        <svg viewBox="0 0 ${W} ${H}" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <linearGradient id="equityGradient" x1="0%" y1="0%" x2="0%" y2="100%">
              <stop offset="0%" stop-color="#2DD4BF" stop-opacity="0.35" />
              <stop offset="100%" stop-color="#2DD4BF" stop-opacity="0" />
            </linearGradient>
          </defs>
          ${yLabels.join('')}
          <path class="chart-fill" d="${fillPath}" />
          ${baselineEl}
          <polyline class="chart-line" points="${linePoints}" />
          ${dots}
          ${xLabels.join('')}
        </svg>`;
    }

    function renderReckoning(sc) {
      sc = sc || { available: false, pairs: [], calls: [] };
      const pairs = sc.pairs || [];
      const calls = sc.calls || [];
      const countEl = document.getElementById('reckoning-count');
      if (countEl) countEl.textContent = '(' + calls.length + ' scored)';

      const fmtTok = (v) => {
        if (!Number.isFinite(v)) return '\u2014';
        const a = Math.abs(v);
        const d = a >= 1000 ? 0 : a >= 1 ? 2 : 6;
        return v.toLocaleString('en-US', { minimumFractionDigits: d, maximumFractionDigits: d });
      };
      const sTok = (v) => (v > 0 ? '+' : '') + fmtTok(v);

      const assetsEl = document.getElementById('reck-assets');
      if (assetsEl) assetsEl.innerHTML = pairs.map((p) => {
        const sym = p.pair.replace('USD', '');
        const tone = p.net_tokens > 0 ? 'up' : p.net_tokens < 0 ? 'down' : '';
        const netCls = p.net_tokens > 0 ? 'pos' : p.net_tokens < 0 ? 'neg' : 'dim';
        const rate = p.decided > 0
          ? Math.round(p.win_rate * 100) + '% (' + p.wins + '/' + p.decided + ')'
          : '(' + p.wins + '/' + p.decided + ')';
        const held = (p.held_tokens && p.held_tokens > 0)
          ? '<div class="reck-card-held dim">held ' + fmtTok(p.held_tokens) + ' ' + sym + '</div>'
          : '';
        return '<div class="reck-card ' + tone + '">'
          + '<div class="reck-card-pair">' + sym + '</div>'
          + '<div class="reck-card-rec"><span class="pos">' + p.wins + '</span>-<span class="neg">' + p.losses + '</span> <span class="dim">' + rate + '</span></div>'
          + '<div class="reck-card-net ' + netCls + '">' + sTok(p.net_tokens) + ' ' + sym + '</div>'
          + held
          + '</div>';
      }).join('');

      const logEl = document.getElementById('reck-log');
      if (!logEl) return;
      if (!calls.length) {
        logEl.innerHTML = '<div class="empty-state"><strong>No calls scored yet.</strong><br>The log fills as Capt\'s reads resolve \u2014 win or wrong, it all goes on the books.</div>';
        return;
      }
      logEl.innerHTML = calls.map((c) => {
        const sym = c.pair.replace('USD', '');
        const v = c.verdict === 'correct' ? '\u2713' : c.verdict === 'wrong' ? '\u2717' : '\u2022';
        const reason = (c.exit_reason || 'manual').replace('_', ' ');
        const netCls = c.token_delta > 0 ? 'pos' : c.token_delta < 0 ? 'neg' : 'dim';
        return '<div class="reck-row reck-' + c.verdict + '">'
          + '<span class="reck-verdict">' + v + '</span>'
          + '<span><strong>' + c.pair + '</strong></span>'
          + '<span class="muted reck-px">' + fmtPrice(c.fill_price) + ' \u2192 ' + fmtPrice(c.exit_price) + '</span>'
          + '<span class="reck-reason"><span class="reason-chip reason-' + (c.exit_reason || 'manual') + '">' + reason + '</span></span>'
          + '<span class="reck-tok ' + netCls + '">' + sTok(c.token_delta) + ' ' + sym + '</span>'
          + '<span class="' + pnlClass(c.pnl_usd) + '" style="text-align:right">' + fmtUsdSigned(c.pnl_usd) + '</span>'
          + '</div>';
      }).join('');
    }

    function renderCloses(closes) {
      const container = document.getElementById('closes');
      document.getElementById('closes-count').textContent = '(' + closes.length + ')';
      if (closes.length === 0) {
        container.innerHTML = `
          <div class="empty-state">
            <strong>No closes filed yet.</strong><br>
            Positions exit on <span class="pos">take-profit</span>, <span class="neg">invalidation</span>, or <span style="color: var(--gold);">time-stop</span>.
            <div class="quote">"The marker returns to the deck when the book stops earning it."</div>
          </div>`;
        return;
      }
      container.innerHTML = closes.map(c => {
        const reasonClass = 'reason-' + (c.exit_reason || 'manual');
        const reasonLabel = (c.exit_reason || 'manual').replace('_', ' ');
        const pnlHtml = c.pnl_usd !== null
          ? '<span class="' + pnlClass(c.pnl_usd) + '">' + fmtUsdSigned(c.pnl_usd) + ' (' + fmtPct(c.pnl_pct) + ')</span>'
          : '—';
        return `
          <div class="close-row">
            <span class="muted">#${c.trade_id}</span>
            <span><strong>${c.pair}</strong> <span class="muted">${c.size_label}</span></span>
            <span><span class="reason-chip ${reasonClass}">${reasonLabel}</span></span>
            <span class="muted">${fmtPrice(c.fill_price)} → ${fmtPrice(c.exit_price)}</span>
            <span style="text-align: right;">${pnlHtml}</span>
          </div>`;
      }).join('');
    }

    function renderDecisions(decisions) {
      const container = document.getElementById('decisions');
      document.getElementById('decisions-count').textContent = '(' + decisions.length + ')';
      if (decisions.length === 0) {
        container.innerHTML = `
          <div class="empty-state">
            <strong>Capt is watching.</strong><br>
            Decision history populates after the next watch.
          </div>`;
        return;
      }
      container.innerHTML = decisions.map(d => {
        const actionTxt = (d.action || '—').toUpperCase();
        const actionClass = d.action === 'enter' ? 'action-enter' : 'action-hold';
        const actionLabel = d.action === 'enter' && d.pair
          ? `${actionTxt} ${d.pair} <span class="muted">${d.size || ''}</span>`
          : actionTxt;
        const thesis = (d.thesis || '').trim();
        const conf = d.confidence ? 'Confidence: ' + d.confidence : '';
        return `
          <div class="decision">
            <div class="decision-head">
              <span class="decision-runid">#${d.run_id}</span>
              <span class="decision-action ${actionClass}">${actionLabel}</span>
              <span class="decision-type">${d.run_type}</span>
              <span class="decision-time">${fmtCtTime(d.run_ts_utc)}</span>
            </div>
            ${thesis ? `<div class="decision-thesis">${escapeHtml(thesis)}</div>` : ''}
            ${conf ? `<div class="decision-confidence">${conf}</div>` : ''}
          </div>`;
      }).join('');
    }

    function renderPlankWalks(walks) {
      const section = document.getElementById('plank-walks-section');
      const container = document.getElementById('plank-walks');
      document.getElementById('plank-walks-count').textContent = '(' + walks.length + ')';
      if (walks.length === 0) {
        section.style.display = 'none';
        return;
      }
      section.style.display = '';
      container.innerHTML = walks.map(w => {
        const reasonLabel = (w.reason || 'unknown').replace(/_/g, ' ');
        const dayStr = w.days_alive ? `${w.days_alive} day${w.days_alive === 1 ? '' : 's'} alive` : null;
        const tradeStr = (w.total_trades !== null && w.total_trades !== undefined) ? `${w.total_trades} total trade${w.total_trades === 1 ? '' : 's'}` : null;
        const winnerStr = (w.biggest_winner_usd !== null && w.biggest_winner_usd !== undefined)
          ? `biggest winner ${fmtUsdSigned(w.biggest_winner_usd)}` : null;
        const chopStr = (w.biggest_chop_usd !== null && w.biggest_chop_usd !== undefined)
          ? `biggest chop ${fmtUsdSigned(w.biggest_chop_usd)}` : null;
        const detailParts = [dayStr, tradeStr, winnerStr, chopStr].filter(Boolean);
        return `
          <div class="plank-walk">
            <div class="plank-walk-head">☠ Plank walked ${fmtCtTime(w.ts_utc)} <span class="muted" style="font-weight: 400; font-size: 12px;">· ${escapeHtml(reasonLabel)}</span></div>
            <div class="plank-walk-detail">
              Reset to ${fmtUsd(w.ending_equity_usd)}
              ${detailParts.length > 0 ? '· ' + detailParts.join(' · ') : ''}
            </div>
          </div>`;
      }).join('');
    }

    function escapeHtml(s) {
      return String(s)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
    }

    async function fetchJson(path) {
      const r = await fetch(path, { cache: 'no-store' });
      if (!r.ok) throw new Error(`${path} returned ${r.status}`);
      return r.json();
    }

    // "From the Bridge" — full Bridge Log narration on the dashboard.
    //
    // Capt's log format has shifted over time — sometimes wrapped in --- rules,
    // sometimes not. Rather than depend on separators, this parser extracts
    // by PATTERN:
    //   1. The first line starting with 📡 (or "Bridge Log —") is the header
    //   2. Lines after the header up to the 🏴 flag are the body
    //   3. The 🏴 flag onward is the attribution
    // Any `---` decorative lines anywhere in the log are stripped from the body.
    function parseBridgeLog(text) {
      if (!text || typeof text !== 'string') return null;
      const normalized = text.replace(/\r\n/g, '\n').trim();
      const allLines = normalized.split('\n');

      // Find header line — first line with 📡 or "Bridge Log —"
      let headerIdx = -1;
      for (let i = 0; i < allLines.length; i++) {
        if (/📡|Bridge\s*Log\s*[—-]/i.test(allLines[i])) {
          headerIdx = i;
          break;
        }
      }
      // Find flag line — first line starting with 🏴 (the pirate flag)
      let flagIdx = -1;
      for (let i = (headerIdx + 1); i < allLines.length; i++) {
        if (/^🏴/.test(allLines[i].trim())) {
          flagIdx = i;
          break;
        }
      }

      // Pull the timestamp out of the header
      let stamp = '';
      if (headerIdx >= 0) {
        const m = allLines[headerIdx].match(/Bridge\s*Log\s*[—-]\s*(.+)$/i);
        stamp = m ? m[1].trim() : allLines[headerIdx].replace(/^📡\s*/, '').trim();
      }

      // Body = lines strictly between header and flag, stripping decorative ---
      const bodyStart = headerIdx >= 0 ? headerIdx + 1 : 0;
      const bodyEnd = flagIdx >= 0 ? flagIdx : allLines.length;
      const bodyLines = allLines.slice(bodyStart, bodyEnd)
        .filter(ln => !/^\s*---+\s*$/.test(ln));  // drop --- decorative rules
      const bodyText = bodyLines.join('\n').trim();
      const bodyParagraphs = bodyText.split(/\n{2,}/).map(p => p.trim()).filter(Boolean);

      // Attribution = flag line onward; pull the flag, byline goes below
      let flag = '🏴‍☠️', byline = '';
      if (flagIdx >= 0) {
        flag = allLines[flagIdx].trim() || '🏴‍☠️';
        const attribLines = allLines.slice(flagIdx + 1)
          .map(s => s.trim())
          .filter(s => s.length > 0 && !/^---+$/.test(s));
        byline = attribLines.join('\n');
      }

      return { stamp, bodyParagraphs, flag, byline };
    }

    function renderBridgeLog(payload) {
      const container = document.getElementById('bridge');
      const status    = document.getElementById('bridge-status');
      if (!payload || !payload.available) {
        const reason = payload && payload.reason ? payload.reason : '';
        container.innerHTML = `
          <div class="empty-state">
            <strong>From the Bridge — awaiting first log.</strong><br>
            Capt's most recent watch narration lands here as soon as the first Bridge Log fires.
            ${reason ? '<div class="quote">' + escapeHtml(reason) + '</div>' : ''}
          </div>`;
        status.textContent = '— pending';
        return;
      }
      const parsed = parseBridgeLog(payload.log_text);
      if (!parsed || (parsed.bodyParagraphs.length === 0 && !parsed.stamp)) {
        container.innerHTML = `<div class="empty-state"><strong>Log parse failed.</strong><br>Raw text saved in the database; the dashboard couldn't structure it.</div>`;
        status.textContent = '— parse failed';
        return;
      }
      const stampHtml = parsed.stamp
        ? `<span class="bridge-stamp">📡 ${escapeHtml(parsed.stamp)}</span>`
        : `<span class="bridge-stamp">📡 Most recent log</span>`;
      const runHtml = `<span class="bridge-run">Run #${payload.run_id} · ${escapeHtml(payload.run_type || 'organic')}</span>`;
      const bodyHtml = parsed.bodyParagraphs.length
        ? parsed.bodyParagraphs.map(p => `<p>${escapeHtml(p)}</p>`).join('')
        : '<p class="dim">No body text in this log.</p>';
      // Byline rendering: usually "— Capt. Crawl" line and "B4E" line.
      let bylineHtml = '';
      if (parsed.byline) {
        const bylineLines = parsed.byline.split('\n').map(s => s.trim()).filter(Boolean);
        const top = bylineLines[0] || '';
        const rest = bylineLines.slice(1).join(' · ');
        bylineHtml = `<span class="by">${escapeHtml(top)}</span>` + (rest ? `<span class="b4e">${escapeHtml(rest)}</span>` : '');
      } else {
        bylineHtml = '<span class="by">— Capt. Crawl</span><span class="b4e">B4E</span>';
      }
      container.innerHTML = `
        <div class="bridge-wrap">
          <div class="bridge-meta">
            ${stampHtml}
            ${runHtml}
          </div>
          <div class="bridge-body">${bodyHtml}</div>
          <div class="bridge-flag">${escapeHtml(parsed.flag || '🏴‍☠️')}</div>
          <div class="bridge-attribution">${bylineHtml}</div>
        </div>`;
      status.textContent = payload.ts ? fmtCtTime(payload.ts) : '—';
    }

    // Reasoning panel ("Capt's Read") — per-pair stance / confidence /
    // multi-timeframe signal alignment / two factor bullets in Capt's voice.
    // Cards mirror the markets ticker layout (same pair accent colors via
    // the .t-hbar / .t-btc / .t-dog / .t-sol / .t-sui modifier classes).
    const STANCE_LABEL = {
      stack:  'STACK',
      buy:    'BUY',
      hold:   'HOLD',
      sell:   'SELL',
      rotate: 'ROTATE',
      watch:  'WATCH',
    };
    const SIGNAL_LABEL = {
      bull:    'Bullish',
      neutral: 'Neutral',
      bear:    'Bearish',
    };
    const SIGNAL_ARROW = {
      bull:    '▲',
      neutral: '━',
      bear:    '▼',
    };
    const TF_LABEL = { intraday: '6h', swing: '7d', macro: '30d' };

    function renderSignalPill(signalVal, tfKey) {
      const cls = signalVal && SIGNAL_LABEL[signalVal]
        ? 'signal-' + signalVal
        : 'signal-neutral';
      const arrow = signalVal ? (SIGNAL_ARROW[signalVal] || '·') : '·';
      const label = signalVal && SIGNAL_LABEL[signalVal] ? SIGNAL_LABEL[signalVal] : '—';
      return `
        <div class="signal-pill ${cls}">
          <span class="signal-arrow">${arrow}</span>
          <span class="signal-label">${label}</span>
          <span class="signal-tf">${TF_LABEL[tfKey] || tfKey}</span>
        </div>`;
    }

    function renderReasoning(reasoning) {
      const container = document.getElementById('reasoning');
      const status    = document.getElementById('reasoning-status');

      if (!reasoning || !reasoning.available) {
        const reason = reasoning && reasoning.reason ? reasoning.reason : '';
        container.className = '';
        container.innerHTML = `
          <div class="empty-state">
            <strong>Capt's Read populates after the next watch.</strong><br>
            Per-pair stance, signal alignment, and reasoning land here once the next Bridge Log fires.
            ${reason ? '<div class="quote">' + escapeHtml(reason) + '</div>' : ''}
          </div>`;
        status.textContent = '— pending';
        return;
      }

      // Two semantic rows: stack targets first (what we're trying to grow),
      // then vehicles (what we're using to grow it). This mirrors the agent's
      // own architecture — see DECISION_SYSTEM_PROMPT in watch.js.
      const STACK_PAIRS   = ['HBARUSD', 'DOGUSD'];
      const VEHICLE_PAIRS = ['BTCUSD', 'SOLUSD', 'SUIUSD'];
      const byPair = {};
      for (const r of reasoning.pairs || []) byPair[r.pair] = r;

      const cardFor = (pair) => {
        const meta = PAIR_META[pair] || { symbol: pair, desc: '', cls: '' };
        const r = byPair[pair];

        if (!r) {
          return `
            <div class="read-card ${meta.cls}">
              <div class="read-head">
                <span class="read-symbol">${meta.symbol}</span>
                <span class="read-desc">${meta.desc}</span>
              </div>
              <div class="read-stance-row">
                <span class="stance-badge stance-watch">— NO READ</span>
                <span class="read-confidence">awaiting next fire</span>
              </div>
            </div>`;
        }

        const stanceKey   = r.stance && STANCE_LABEL[r.stance] ? r.stance : 'watch';
        const stanceLabel = STANCE_LABEL[stanceKey];
        const confKey     = r.confidence || '';
        const confHtml    = r.confidence
          ? `confidence: <span class="conf-value ${confKey}">${r.confidence}</span>`
          : 'confidence: <span class="conf-value">—</span>';

        const signalsHtml = `
          ${renderSignalPill(r.signals?.intraday, 'intraday')}
          ${renderSignalPill(r.signals?.swing,    'swing')}
          ${renderSignalPill(r.signals?.macro,    'macro')}
        `;

        const factorsHtml = (r.factors && r.factors.length > 0)
          ? `<div class="read-factors">
               ${r.factors.map(f => `<div class="read-factor">${escapeHtml(f)}</div>`).join('')}
             </div>`
          : '';

        const footerHtml = r.ts
          ? `<div class="read-footer">Read · ${fmtCtTime(r.ts)}${r.run_id ? ' · run #' + r.run_id : ''}</div>`
          : '';

        return `
          <div class="read-card ${meta.cls}">
            <div class="read-head">
              <span class="read-symbol">${meta.symbol}</span>
              <span class="read-desc">${meta.desc}</span>
            </div>
            <div class="read-stance-row">
              <span class="stance-badge stance-${stanceKey}">${stanceLabel}</span>
              <span class="read-confidence">${confHtml}</span>
            </div>
            <div class="read-signals">${signalsHtml}</div>
            ${factorsHtml}
            ${footerHtml}
          </div>`;
      };

      const stackCards   = STACK_PAIRS.map(cardFor).join('');
      const vehicleCards = VEHICLE_PAIRS.map(cardFor).join('');

      container.className = '';
      container.innerHTML = `
        <div class="reads-divider stack-targets">Stack targets · what we're growing</div>
        <div class="reads-stack">${stackCards}</div>
        <div class="reads-divider">Vehicles · what we're using to grow it</div>
        <div class="reads-vehicles">${vehicleCards}</div>
      `;

      // Compact status summary in the section header — counts pairs that
      // actually have a fresh read this fire.
      const allPairs = [...STACK_PAIRS, ...VEHICLE_PAIRS];
      const fresh = allPairs.filter(p => byPair[p]).length;
      status.textContent = `${fresh}/5 pairs · last read ${fmtCtTime(reasoning.ts || (reasoning.pairs?.[0]?.ts) || new Date().toISOString())}`;
    }

    // ===========================================================================
    // Chain Activity — HBAR + DOG on-chain panels.
    // Renders two side-by-side cards. When a feed is down for the latest watch,
    // the corresponding card dims and shows a single "data unavailable" line —
    // Option 1 fail-forward, no stale fallback. When the 7-day baseline has
    // enough samples (≥6), each metric also shows its delta vs baseline.
    // ===========================================================================
    function formatBigNum(n) {
      if (n === null || n === undefined || !Number.isFinite(n)) return '—';
      const abs = Math.abs(n);
      if (abs >= 1e9) return (n / 1e9).toFixed(2) + 'B';
      if (abs >= 1e6) return (n / 1e6).toFixed(2) + 'M';
      if (abs >= 1e3) return (n / 1e3).toFixed(2) + 'K';
      if (abs >= 1)   return n.toLocaleString();
      return n.toFixed(4);
    }
    function formatDeltaPct(current, baseline) {
      if (!Number.isFinite(current) || !Number.isFinite(baseline) || baseline === 0) return null;
      return ((current - baseline) / baseline) * 100;
    }
    function chainRow(label, value, deltaInfo) {
      const v = (value === null || value === undefined) ? '<span class="muted">—</span>' : escapeHtml(String(value));
      let deltaHtml = '';
      if (deltaInfo && Number.isFinite(deltaInfo.pct)) {
        const sign = deltaInfo.pct >= 0 ? '+' : '';
        const cls  = deltaInfo.pct >= 0 ? 'up' : 'down';
        const baselineNote = deltaInfo.baselineLabel ? ` <span class="muted">vs ${deltaInfo.baselineLabel}</span>` : '';
        deltaHtml = `<span class="chain-row-delta ${cls}">${sign}${deltaInfo.pct.toFixed(1)}%${baselineNote}</span>`;
      } else if (deltaInfo && deltaInfo.note) {
        deltaHtml = `<span class="chain-row-delta">${escapeHtml(deltaInfo.note)}</span>`;
      }
      return `
        <div class="chain-row">
          <span class="chain-row-label">${escapeHtml(label)}</span>
          <span class="chain-row-value">${v}</span>
          ${deltaHtml}
        </div>`;
    }

    function renderOnchain(onchain, tickers) {
      const container = document.getElementById('onchain');
      const status    = document.getElementById('onchain-status');

      if (!onchain || !onchain.available) {
        container.innerHTML = `
          <div class="empty-state">
            <strong>Chain Activity populates after the next watch.</strong><br>
            On-chain context from Hedera Mirror Node (HBAR) and Unisat (DOG) lands here once the next Bridge Log fires.
          </div>`;
        status.textContent = '— pending';
        return;
      }

      // DOG's USD price is sourced from Kraken's live ticker (same source the
      // Markets strip shows). The on-chain card pulls it from there rather
      // than deriving from Unisat data so the number matches what the user
      // sees elsewhere on the page. Sat-floor stays prominent as the runes-
      // native denomination; both live on the same row.
      const tickerArray = Array.isArray(tickers) ? tickers : (tickers?.tickers || []);
      const dogTicker   = tickerArray.find(t => t && t.pair === 'DOGUSD') || null;
      const dogUsdPrice = (dogTicker && Number.isFinite(dogTicker.last)) ? dogTicker.last : null;

      const renderHbarCard = () => {
        const h = onchain.hbar;
        if (!h || h.status === 'failed' || (!h.tps_avg && !h.total_supply)) {
          return `
            <div class="chain-card t-hbar is-down">
              <div class="chain-head">
                <span class="chain-symbol">HBAR · HEDERA NETWORK</span>
                <span class="chain-status failed">data unavailable</span>
              </div>
              <div class="chain-empty">Mirror Node feed was unreachable for this watch. No on-chain context for HBAR — Capt reasoned from market data only.</div>
              <div class="chain-footer">source: Hedera Mirror Node · ${fmtCtTime(onchain.ts)}</div>
            </div>`;
        }
        const baselineReady = h.baseline && h.baseline.ready;
        const baselineLabel = baselineReady ? `7d (n=${h.baseline.sample_count})` : null;
        const rows = [];
        if (Number.isFinite(h.tps_avg)) {
          const deltaInfo = baselineReady
            ? { pct: formatDeltaPct(h.tps_avg, h.baseline.avg_tps), baselineLabel }
            : (h.baseline && h.baseline.sample_count > 0 ? { note: `baseline building (n=${h.baseline.sample_count}/6)` } : { note: 'first observation' });
          rows.push(chainRow('Network TPS', h.tps_avg.toFixed(2), deltaInfo));
        }
        if (Number.isFinite(h.total_tx) && Number.isFinite(h.window_secs)) {
          const winMin = (h.window_secs / 60).toFixed(1);
          rows.push(chainRow('Tx in window', `${h.total_tx.toLocaleString()} (${winMin} min)`, null));
        }
        if (Number.isFinite(h.total_fees_hbar) && Number.isFinite(h.window_secs) && h.window_secs > 0) {
          const feesEst = h.fees_estimated === 1;
          const rrHbarDay = h.total_fees_hbar * 86400 / h.window_secs;
          const rrUsdDay = Number.isFinite(h.price_usd) ? rrHbarDay * h.price_usd : null;
          const feesVal = rrUsdDay != null
            ? `${feesEst ? '~' : ''}$${Math.round(rrUsdDay).toLocaleString()}/day`
            : `${feesEst ? '~' : ''}${formatBigNum(rrHbarDay)} ℏ/day`;
          const feesN = h.baseline ? (h.baseline.fees_sample_count || 0) : 0;
          const feesReady = feesN >= 6;
          const feesDelta = feesReady
            ? { pct: formatDeltaPct(h.total_fees_hbar, h.baseline.avg_fees), baselineLabel: `7d (n=${feesN})` }
            : (feesN > 0 ? { note: `baseline building (n=${feesN}/6)` } : { note: 'first observation' });
          rows.push(chainRow('Network fees (run-rate)', feesVal, feesDelta));
        }
        if (Number.isFinite(h.released_supply) && Number.isFinite(h.total_supply)) {
          const circB = (h.released_supply / 1e9).toFixed(2);
          const totB  = (h.total_supply / 1e9).toFixed(2);
          rows.push(chainRow('HBAR supply', `${circB}B / ${totB}B`, null));
        }
        const statusLabel = h.status === 'ok' ? 'live' : h.status;
        return `
          <div class="chain-card t-hbar">
            <div class="chain-head">
              <span class="chain-symbol">HBAR · HEDERA NETWORK</span>
              <span class="chain-status ${h.status}">${statusLabel}</span>
            </div>
            <div class="chain-metrics">${rows.join('')}</div>
            <div class="chain-footer">source: Hedera Mirror Node · ${fmtCtTime(onchain.ts)}${onchain.run_id ? ' · run #' + onchain.run_id : ''}</div>
          </div>`;
      };

      const renderDogCard = () => {
        const d = onchain.dog;
        if (!d || d.status === 'failed' || (d.holders === null && d.current_price_sats === null)) {
          return `
            <div class="chain-card t-dog is-down">
              <div class="chain-head">
                <span class="chain-symbol">DOG · BITCOIN RUNES</span>
                <span class="chain-status failed">data unavailable</span>
              </div>
              <div class="chain-empty">Unisat feed was unreachable for this watch. No on-chain context for DOG — Capt reasoned from market data only.</div>
              <div class="chain-footer">source: Unisat · ${fmtCtTime(onchain.ts)}</div>
            </div>`;
        }
        // Row order reflects what a DOG holder actually scans for. USD price
        // leads (the number most users think in), with the runes-native sat-
        // floor + 24h change right alongside in the delta column. Sat-floor
        // is the more conviction-relevant number (it's the rune's value
        // against BTC), but USD is the universal entry point.
        const baselineReady = d.baseline && d.baseline.ready;
        const baselineLabel = baselineReady ? `7d (n=${d.baseline.sample_count})` : null;
        const rows = [];

        // PRICE row — USD primary (from Kraken ticker so it matches the rest
        // of the dashboard), sat-floor + 24h sat-price change as secondary.
        if (Number.isFinite(dogUsdPrice) || Number.isFinite(d.current_price_sats)) {
          let priceValue, priceNote = null;
          if (Number.isFinite(dogUsdPrice)) {
            // DOG prices are tiny — 7 decimals captures the meaningful digits.
            priceValue = '$' + dogUsdPrice.toFixed(7);
            const satParts = [];
            if (Number.isFinite(d.current_price_sats)) satParts.push(d.current_price_sats + ' sats');
            if (Number.isFinite(d.change_price_24h)) {
              const sign = d.change_price_24h >= 0 ? '+' : '';
              satParts.push(sign + d.change_price_24h.toFixed(2) + '% (24h sat-floor)');
            }
            if (satParts.length > 0) priceNote = { note: satParts.join(' · ') };
          } else {
            // USD unavailable — fall back to sat-floor as the primary value.
            priceValue = d.current_price_sats + ' sats';
            if (Number.isFinite(d.change_price_24h)) {
              const sign = d.change_price_24h >= 0 ? '+' : '';
              priceNote = { note: sign + d.change_price_24h.toFixed(2) + '% (24h)' };
            }
          }
          rows.push(chainRow('Price', priceValue, priceNote));
        }

        if (Number.isFinite(d.holders)) {
          let deltaInfo = null;
          if (baselineReady && Number.isFinite(d.baseline.avg_holders)) {
            deltaInfo = { pct: formatDeltaPct(d.holders, d.baseline.avg_holders), baselineLabel };
          } else if (d.baseline && d.baseline.sample_count > 0) {
            deltaInfo = { note: `baseline building (n=${d.baseline.sample_count}/6)` };
          } else {
            deltaInfo = { note: 'first observation' };
          }
          rows.push(chainRow('Holders', d.holders.toLocaleString(), deltaInfo));
        }
        if (Number.isFinite(d.transactions)) {
          rows.push(chainRow('24h transactions', d.transactions.toLocaleString(), null));
        }
        if (Number.isFinite(d.btc_volume_24h)) {
          // Marketplace volume is naturally BTC-denominated (it's the medium
          // of exchange on the runes marketplace), with sats as the on-chain-
          // native unit alongside.
          const btcStr = d.btc_volume_24h < 0.01
            ? d.btc_volume_24h.toFixed(6) + ' BTC'
            : d.btc_volume_24h.toFixed(4) + ' BTC';
          const satsNote = Number.isFinite(d.btc_volume_24h_sats)
            ? { note: Math.round(d.btc_volume_24h_sats).toLocaleString() + ' sats' }
            : null;
          rows.push(chainRow('24h marketplace vol', btcStr, satsNote));
        }
        if (Number.isFinite(d.market_cap_btc) || Number.isFinite(d.market_cap_usd)) {
          // Market cap with USD primary (universal frame), BTC alongside.
          let capValue, capNote = null;
          if (Number.isFinite(d.market_cap_usd) && d.market_cap_usd > 0) {
            const usdM = d.market_cap_usd / 1e6;
            capValue = '$' + usdM.toFixed(2) + 'M';
            if (Number.isFinite(d.market_cap_btc)) {
              capNote = { note: d.market_cap_btc.toFixed(2) + ' BTC' };
            }
          } else if (Number.isFinite(d.market_cap_btc)) {
            capValue = d.market_cap_btc.toFixed(2) + ' BTC';
          }
          if (capValue) rows.push(chainRow('Market cap', capValue, capNote));
        }
        return `
          <div class="chain-card t-dog">
            <div class="chain-head">
              <span class="chain-symbol">DOG · BITCOIN RUNES</span>
              <span class="chain-status ok">live</span>
            </div>
            <div class="chain-metrics">${rows.join('')}</div>
            <div class="chain-footer">source: Unisat · ${fmtCtTime(onchain.ts)}${onchain.run_id ? ' · run #' + onchain.run_id : ''}</div>
          </div>`;
      };

      container.innerHTML = `
        <div class="chain-grid">
          ${renderHbarCard()}
          ${renderDogCard()}
        </div>`;

      // Header status summary — count how many feeds were live for this watch.
      const live = (onchain.hbar?.status === 'ok' ? 1 : 0) + (onchain.dog?.status === 'ok' ? 1 : 0);
      status.textContent = `${live}/2 feeds live · ${fmtCtTime(onchain.ts || new Date().toISOString())}`;
    }

    function renderStack(stack) {
      const container = document.getElementById('stack');
      const status = document.getElementById('stack-status');
      if (!stack || !stack.available) {
        container.innerHTML = `
          <div class="empty-state">
            <strong>Stack baseline pending.</strong><br>
            Stack progress activates after the first Bridge Log writes a baseline snapshot.
            ${stack && stack.reason ? '<div class="quote">' + escapeHtml(stack.reason) + '</div>' : ''}
          </div>`;
        status.textContent = '— pending';
        return;
      }
      const targets = stack.targets || {};
      const formatUnits = (n) => {
        if (Math.abs(n) >= 1e6) return (n / 1e6).toFixed(2) + 'M';
        if (Math.abs(n) >= 1e3) return (n / 1e3).toFixed(2) + 'K';
        if (Math.abs(n) >= 1)   return n.toFixed(2);
        return n.toFixed(6);
      };
      const rows = Object.entries(targets).map(([sym, t]) => {
        if (!t.available) {
          return `<div class="stack-row"><strong>${sym}</strong><span class="muted">stack data unavailable</span></div>`;
        }
        const sign = t.deltaUnits >= 0 ? '+' : '';
        const cls = t.deltaUnits >= 0 ? 'pos' : 'neg';
        return `
          <div class="stack-row">
            <div class="stack-sym"><strong>${sym}</strong></div>
            <div class="stack-now">${formatUnits(t.currentEquivalent)} ${sym}<br><span class="muted">equiv now @ $${t.currentPrice}</span></div>
            <div class="stack-was">${formatUnits(t.baselineEquivalent)} ${sym}<br><span class="muted">baseline @ $${t.baselinePrice}</span></div>
            <div class="stack-delta ${cls}">${sign}${formatUnits(t.deltaUnits)} ${sym}<br><span class="${cls}">${sign}${t.deltaPct.toFixed(2)}%</span></div>
          </div>`;
      }).join('');
      container.innerHTML = `<div class="stack-grid">${rows}</div>`;
      const targetSummary = Object.entries(targets)
        .filter(([_, t]) => t.available)
        .map(([sym, t]) => {
          const sign = t.deltaPct >= 0 ? '+' : '';
          return `${sym} ${sign}${t.deltaPct.toFixed(2)}%`;
        }).join(' · ');
      status.textContent = targetSummary || '—';
    }

    async function refreshLive() {
      try {
        const [state, tickers, stack, reasoning, bridge, onchain] = await Promise.all([
          fetchJson('/api/state'),
          fetchJson('/api/tickers').catch(() => ({ tickers: [] })),
          fetchJson('/api/stack').catch((e) => ({ available: false, reason: e.message })),
          fetchJson('/api/reasoning').catch((e) => ({ available: false, reason: e.message })),
          fetchJson('/api/latest-log').catch((e) => ({ available: false, reason: e.message })),
          fetchJson('/api/onchain').catch((e) => ({ available: false, reason: e.message })),
        ]);
        renderState(state);
        renderTickers(tickers.tickers || []);
        renderReasoning(reasoning);
        renderOnchain(onchain, tickers.tickers || []);
        renderStack(stack);
        renderBridgeLog(bridge);
        document.getElementById('refresh-status').innerHTML =
          'live <span class="heart">●</span> ' +
          new Date().toLocaleTimeString('en-US', { hour12: false, timeZone: 'America/Chicago' }) + ' CT';
      } catch (e) {
        document.getElementById('refresh-status').textContent = 'refresh failed: ' + e.message;
        console.error(e);
      }
    }

    async function refreshHistory() {
      try {
        const [equity, closes, decisions, planks, reckoning] = await Promise.all([
          fetchJson('/api/equity'),
          fetchJson('/api/closes?limit=20'),
          fetchJson('/api/decisions?limit=20'),
          fetchJson('/api/plank-walks'),
          fetchJson('/api/scorecard').catch(() => ({ available: false, pairs: [], calls: [] })),
        ]);
        renderEquity(equity.snapshots || []);
        renderCloses(closes.closes || []);
        renderDecisions(decisions.decisions || []);
        renderPlankWalks(planks.plank_walks || []);
        renderReckoning(reckoning || { available: false, pairs: [], calls: [] });
      } catch (e) {
        console.error('refreshHistory failed:', e);
      }
    }

    // ─── The Chart — price + watched levels + fills (Kraken-CLI spine) ───
    const THESIS_PAIRS = ['BTCUSD','HBARUSD','DOGUSD','SOLUSD','SUIUSD'];
    const THESIS_TFS = [{ label:'15m', value:15 }, { label:'4h', value:240 }, { label:'1d', value:1440 }];
    let tChart = null, tSeries = null, tPriceLines = [], tTradeSeries = [], tTrades = [], tPrec = 2, tTooltip = null, tLastView = '';
    let tPair = 'BTCUSD', tInterval = 240;

    function initThesisChart() {
      const host = document.getElementById('thesis-chart');
      if (!host || !window.LightweightCharts) return;
      tChart = LightweightCharts.createChart(host, {
        autoSize: true,
        layout: { background: { type: 'solid', color: 'transparent' }, textColor: '#8b949e',
                  fontFamily: "'SF Mono','JetBrains Mono',Menlo,monospace", fontSize: 11 },
        grid: { vertLines: { color: 'rgba(42,47,59,0.4)' }, horzLines: { color: 'rgba(42,47,59,0.4)' } },
        rightPriceScale: { borderColor: '#2a2f3b' },
        timeScale: { borderColor: '#2a2f3b', timeVisible: true, secondsVisible: false },
        crosshair: { mode: LightweightCharts.CrosshairMode.Normal },
      });
      tSeries = tChart.addCandlestickSeries({
        upColor: '#2DD4BF', downColor: '#FB7185',
        wickUpColor: '#2DD4BF', wickDownColor: '#FB7185',
        borderUpColor: '#2DD4BF', borderDownColor: '#FB7185',
      });
      const pairWrap = document.getElementById('thesis-pairs');
      pairWrap.innerHTML = THESIS_PAIRS.map(p =>
        `<button class="seg-btn${p===tPair?' active':''}" data-pair="${p}">${p.replace('USD','')}</button>`).join('');
      pairWrap.querySelectorAll('button').forEach(b => b.addEventListener('click', () => {
        tPair = b.dataset.pair;
        pairWrap.querySelectorAll('button').forEach(x => x.classList.toggle('active', x===b));
        refreshThesisChart();
      }));
      const tfWrap = document.getElementById('thesis-tfs');
      tfWrap.innerHTML = THESIS_TFS.map(t =>
        `<button class="seg-btn${t.value===tInterval?' active':''}" data-tf="${t.value}">${t.label}</button>`).join('');
      tfWrap.querySelectorAll('button').forEach(b => b.addEventListener('click', () => {
        tInterval = parseInt(b.dataset.tf, 10);
        tfWrap.querySelectorAll('button').forEach(x => x.classList.toggle('active', x===b));
        refreshThesisChart();
      }));

      tTooltip = document.createElement('div');
      tTooltip.className = 'tchart-tip';
      tTooltip.style.display = 'none';
      host.appendChild(tTooltip);
      tChart.subscribeCrosshairMove((param) => {
        if (!param || !param.point || param.time === undefined) { tTooltip.style.display = 'none'; return; }
        const hit = tTrades.find(tr => param.time >= tr.t0 && param.time <= tr.t1);
        if (!hit) { tTooltip.style.display = 'none'; return; }
        tTooltip.innerHTML = hit.html;
        tTooltip.style.display = 'block';
        let x = param.point.x + 14, y = param.point.y + 14;
        if (x + tTooltip.offsetWidth > host.clientWidth) x = param.point.x - tTooltip.offsetWidth - 14;
        if (y + tTooltip.offsetHeight > host.clientHeight) y = param.point.y - tTooltip.offsetHeight - 14;
        tTooltip.style.left = Math.max(0, x) + 'px';
        tTooltip.style.top = Math.max(0, y) + 'px';
      });
    }

    function clearThesisLines() {
      tPriceLines.forEach(l => { try { tSeries.removePriceLine(l); } catch (e) {} });
      tPriceLines = [];
    }

    function drawTheses(theses) {
      clearThesisLines();
      const LS = LightweightCharts.LineStyle;
      const dirColor = { accumulate: '#2DD4BF', trim: '#FB7185', watch: '#61A6DD' };
      (theses || []).filter(t => Number.isFinite(t.level_low)).slice(0, 8).forEach(t => {
        const watching = t.state === 'watching';
        const base = dirColor[t.direction] || '#61A6DD';
        tPriceLines.push(tSeries.createPriceLine({
          price: t.level_low,
          color: watching ? base : 'rgba(110,118,129,0.4)',
          lineWidth: watching ? 2 : 1,
          lineStyle: watching ? LS.Dashed : LS.Dotted,
          axisLabelVisible: watching,
          title: watching ? (t.direction === 'accumulate' ? 'stack' : t.direction) : '',
        }));
      });
    }

    function snapToCandle(sec, times) {
      if (!times.length) return sec;
      let best = times[0];
      for (const ct of times) { if (ct <= sec) best = ct; else break; }
      return best;
    }

    function tFmtPrice(p) {
      if (!Number.isFinite(p)) return '\u2014';
      return Number(p).toLocaleString('en-US', { minimumFractionDigits: tPrec, maximumFractionDigits: tPrec });
    }

    const EXIT_LABEL = { invalidation: 'inval', take_profit: 'TP', plank_walk: 'plank', time_stop: 'time' };

    function clearTradeSeries() {
      tTradeSeries.forEach(s => { try { tChart.removeSeries(s); } catch (e) {} });
      tTradeSeries = [];
      tTrades = [];
    }

    function addSeg(t0, t1, price, color, width, style) {
      if (t0 === t1 || !Number.isFinite(price)) return;
      const s = tChart.addLineSeries({ color, lineWidth: width, lineStyle: style,
        priceLineVisible: false, lastValueVisible: false, crosshairMarkerVisible: false });
      s.setData([{ time: t0, value: price }, { time: t1, value: price }]);
      tTradeSeries.push(s);
    }

    function drawTrades(closes, positions, candleTimes) {
      clearTradeSeries();
      const markers = [];
      if (!candleTimes.length) { tSeries.setMarkers(markers); return; }
      const LS = LightweightCharts.LineStyle;
      const toSec = (iso) => Math.floor(new Date(iso).getTime() / 1000);
      const lastT = candleTimes[candleTimes.length - 1];

      (closes || []).filter(c => c.pair === tPair && c.fill_price && c.ts_utc).forEach(c => {
        const t0 = snapToCandle(toSec(c.ts_utc), candleTimes);
        const hasExit = c.exit_price && c.exit_ts_utc;
        const t1 = hasExit ? snapToCandle(toSec(c.exit_ts_utc), candleTimes) : lastT;
        const win = (c.pnl_usd || 0) >= 0;
        addSeg(t0, t1, c.invalidation_price, 'rgba(251,113,133,0.5)', 1, LS.Dotted);
        addSeg(t0, t1, c.take_profit_price, 'rgba(45,212,191,0.45)', 1, LS.Dotted);
        if (hasExit && t0 !== t1) {
          const v = tChart.addLineSeries({ color: win ? '#2DD4BF' : '#FB7185', lineWidth: 2,
            priceLineVisible: false, lastValueVisible: false, crosshairMarkerVisible: false });
          v.setData([{ time: t0, value: c.fill_price }, { time: t1, value: c.exit_price }]);
          tTradeSeries.push(v);
        }
        markers.push({ time: t0, position: 'belowBar', color: '#61A6DD', shape: 'arrowUp', text: tFmtPrice(c.fill_price) });
        if (hasExit) {
          const reason = EXIT_LABEL[c.exit_reason] || c.exit_reason || 'exit';
          markers.push({ time: t1, position: 'aboveBar', color: win ? '#2DD4BF' : '#FB7185', shape: 'arrowDown', text: reason + ' ' + tFmtPrice(c.exit_price) });
        }
        const pnlPct = Number.isFinite(c.pnl_pct) ? ((c.pnl_pct >= 0 ? '+' : '') + c.pnl_pct.toFixed(2) + '%') : '\u2014';
        const pnlUsd = Number.isFinite(c.pnl_usd) ? ((c.pnl_usd >= 0 ? '+$' : '-$') + Math.abs(c.pnl_usd).toFixed(2)) : '';
        tTrades.push({ t0, t1, html:
          '<div class="tip-h">' + (c.kraken_order_id || 'trade') + ' \u00b7 ' + tPair.replace('USD','') + ' ' + c.side + ' ' + (c.size_label || '') + '</div>' +
          '<div>fill ' + tFmtPrice(c.fill_price) + (hasExit ? ' \u2192 exit ' + tFmtPrice(c.exit_price) : ' \u00b7 open') + '</div>' +
          '<div class="dim">inval ' + tFmtPrice(c.invalidation_price) + ' \u00b7 tp ' + tFmtPrice(c.take_profit_price) + '</div>' +
          (hasExit ? ('<div class="' + (win ? 'pos' : 'neg') + '">' + (EXIT_LABEL[c.exit_reason] || c.exit_reason || '') + ' \u00b7 ' + pnlPct + ' ' + pnlUsd + '</div>') : '') });
      });

      (positions || []).filter(p => p.pair === tPair && p.fill_price && p.opened_at).forEach(p => {
        const t0 = snapToCandle(toSec(p.opened_at), candleTimes);
        addSeg(t0, lastT, p.invalidation_price, 'rgba(251,113,133,0.55)', 1, LS.Dotted);
        addSeg(t0, lastT, p.take_profit_price, 'rgba(45,212,191,0.5)', 1, LS.Dotted);
        markers.push({ time: t0, position: 'belowBar', color: '#F9C015', shape: 'arrowUp', text: tFmtPrice(p.fill_price) });
        tTrades.push({ t0, t1: lastT, html:
          '<div class="tip-h">' + (p.kraken_order_id || 'open') + ' \u00b7 ' + tPair.replace('USD','') + ' ' + p.side + ' ' + (p.size_label || '') + '</div>' +
          '<div>fill ' + tFmtPrice(p.fill_price) + ' \u00b7 open</div>' +
          '<div class="dim">inval ' + tFmtPrice(p.invalidation_price) + '</div>' });
      });

      markers.sort((a, b) => a.time - b.time);
      tSeries.setMarkers(markers);
    }

    function precisionFor(price) {
      const p = Math.abs(Number(price) || 0);
      if (p >= 1000)  return 1;
      if (p >= 1)     return 2;
      if (p >= 0.1)   return 4;
      if (p >= 0.01)  return 5;
      if (p >= 0.001) return 6;
      return 7;
    }

    async function refreshThesisChart() {
      if (!tSeries) return;
      const statusEl = document.getElementById('thesis-chart-status');
      try {
        const [candlesR, thesesR, closesR, stateR] = await Promise.all([
          fetchJson(`/api/candles?pair=${tPair}&interval=${tInterval}`),
          fetchJson(`/api/theses?pair=${tPair}`).catch(() => ({ theses: [] })),
          fetchJson('/api/closes?limit=50').catch(() => ({ closes: [] })),
          fetchJson('/api/state').catch(() => ({ positions: [] })),
        ]);
        const candles = candlesR.candles || [];
        if (candles.length) {
          tPrec = precisionFor(candles[candles.length - 1].close);
          tSeries.applyOptions({ priceFormat: { type: 'price', precision: tPrec, minMove: Math.pow(10, -tPrec) } });
        }
        tSeries.setData(candles);
        tSeries.priceScale().applyOptions({ autoScale: true });
        const times = candles.map(c => c.time);
        drawTheses(thesesR.theses || []);
        drawTrades(closesR.closes || [], stateR.positions || [], times);
        const viewKey = tPair + '|' + tInterval;
        if (viewKey !== tLastView) {
          tLastView = viewKey;
          const visN = ({ 15: 120, 240: 130, 1440: 120 })[tInterval] || 120;
          if (candles.length > visN) tChart.timeScale().setVisibleLogicalRange({ from: candles.length - visN, to: candles.length + 3 });
          else tChart.timeScale().fitContent();
        }
        if (statusEl) statusEl.textContent = `${tPair.replace('USD','')} · ${candles.length} candles`;
      } catch (e) {
        if (statusEl) statusEl.textContent = 'chart unavailable';
        console.error('thesis chart failed:', e);
      }
    }

    refreshLive();
    refreshHistory();
    initThesisChart();
    refreshThesisChart();
    setInterval(refreshLive, 15_000);
    setInterval(refreshHistory, 60_000);
    setInterval(refreshThesisChart, 60_000);
  </script>
</body>
</html>
